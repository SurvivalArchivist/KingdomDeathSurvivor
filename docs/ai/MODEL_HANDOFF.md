# Model Handoff Log

## v3.3.0 Release Preparation

- Prepared version 3.3.0 for the Settlement tab, permanent Campaign/Vignette choice, Vignette survivor templates, Campaign Lantern Year and LAN return history, and production Host/Client startup gate. Survivor schema remains 6 and settlement metadata schema remains 1; older schema-1 settlement records are normalized with the new optional fields.
- Release preflight passed syntax checks and all 262 tests. Tagged multi-platform packaging remains the publication gate. Upgrade LAN hosts and clients together and back up the entire Survivors folder before changing versions.

## Settlement Record Tab

- 2026-09-06: Production now uses Host/Client roles only. New/default and legacy `local` configurations show a blocking startup role page before survivor initialization; choosing Host or Client persists the role and opens Settings for folder/host setup. Packaged builds and ordinary `npm start` hide Local from Settings and reject it in both the main process and provider factory. Added `npm run dev` (`electron . --dev`) as the only supported Local Development entry point; the startup page exposes a third Local Development choice only there. Saved Host/Client roles continue directly into the app. Files: `package.json`, `src/main.js`, `src/preload.js`, `src/renderer.js`, `src/survivorProvider.js`, `ui/components/index.html`, `ui/components/styles/base.css`, tests, and canonical docs. Verification: all 262 tests, syntax checks, and `git diff --check` passed.

- 2026-09-06: Added Campaign Lantern Year and LAN-only survivor return history. The Host can enter a non-negative Lantern Year or advance it by one and save with the settlement settings; Clients view it. Successful `markReturned` saves made through LAN Host or LAN Client add an idempotent `returns` entry to `settlement.json` with survivor ID/name, the captured year, timestamp, and alive/dead state. Local Files still updates survivor `lastReturned` but is forced not to write settlement return history; Vignette hides Campaign year/history. The durable settlement journal now applies knowledge and return metadata together after survivor commit. Files: `src/settlementService.js`, `src/dataService.js`, `src/survivorProvider.js`, `src/lanSurvivorHost.js`, `src/renderer.js`, `ui/components/index.html`, `ui/components/styles/base.css`, related tests, and settlement docs. Verification: all 258 tests, syntax checks, and `git diff --check` passed.

- 2026-09-06: Settlement Type is now a one-time Host choice. The first Settlement settings save persists `settlementTypeLocked: true`; later saves may rename the settlement but cannot switch between Campaign and Vignette. The renderer disables the type selector after that save, explains the permanent choice, and the data/service layers enforce the invariant. Older Campaign records without a lock marker remain selectable; older Vignette/template records are inferred as locked. Verification: all 256 tests passed, including persistence, migration, and renderer lock behavior; syntax and diff checks passed.

- 2026-09-06: Added Settlement Type (`campaign`/`vignette`) to the Settlement tab and settlement record. Campaign keeps existing behavior. Vignette adds host-only `Set Template` and `Restore to Template` actions; setting a template snapshots current survivor JSON records into `settlement.json`, while restore creates a timestamped `settlement-backups/` copy of current survivor JSON, replaces survivors from the template, clears/rebuilds settlement knowledge and journal state from restored survivors, and refreshes the roster. Local Files remains disabled for the Settlement tab and LAN Clients remain read-only for direct settlement actions. Files: `src/settlementService.js`, `src/dataService.js`, `src/survivorProvider.js`, `src/main.js`, `src/preload.js`, `src/renderer.js`, `ui/components/index.html`, `ui/components/styles/base.css`, `test/settlementService.test.js`, `test/survivorProvider.test.js`, `test/renderer.smoke.test.js`, `AGENTS.md`, `docs/ai/PROJECT_CONTEXT.md`, `docs/ai/SETTLEMENT_RECORD.md`, `docs/ai/MODEL_HANDOFF.md`. Verification: `node --check src/main.js src/preload.js src/dataService.js src/renderer.js src/settlementService.js src/survivorProvider.js src/lanSurvivorHost.js`; `npm test`; `git diff --check`.

- Clarified that client survivor saves continue registering permanent knowledge in the host settlement record; only direct tab/name edits are host-only. Added an HTTP-to-filesystem regression test covering a client adding knowledge to an existing survivor and denial of direct settlement edits.

- Renamed the roster navigation/title to Survivors, retaining internal roster IDs and workflows. Added a separate Settlement tab with an editable name and alphabetically sorted, expandable unlocked knowledge definitions.
- LAN Host alone can edit direct settlement settings and Vignette templates. LAN Clients view/refresh; Local Development gets a greyed-out tab with an explanation. Provider enforcement blocks Local/Client direct settlement writes and HTTP settlement PUT returns 403. This follows the selected Host/Client production direction.
- Added optional `name` to the schema-1 settlement record without a version bump. Saves trim/validate the name (200-character limit), check ID/revision after recovery, and atomically preserve discoveries/metadata. Refresh and navigation protect an unsaved name; errors retain the draft. Refresh explicitly reloads authoritative data.
- Verification: all 252 tests passed, including persistence/conflicts, provider permissions, HTTP denial, and renderer role/save behavior. Syntax checks and diff whitespace checks passed. Manual Electron/theme acceptance remains to be done.

## v3.2.1 Release Preparation

- Prepared the 3.2.1 maintenance patch with transitive `fast-uri` 3.1.7, refreshed changelog/release notes, and cleaned obsolete checkpoint material from `AGENTS.md`. Survivor schema remains 6; settlement metadata schema remains 1.
- User confirmed Windows acceptance testing passed for the setup and portable builds with Electron 41.10.4 and Electron Builder 26.15.7. The 3.2.1 commit is ready; tagging, pushing, and release publication remain separate steps.
- The settlement knowledge and Showdown refactor acceptance gates were completed for 3.2.0. Upgrade LAN host and clients together and back up the entire Survivors folder.

## Settlement Knowledge Record — Latest Work

- Final verification passed 246/246 tests (1 syntax, 159 integration, 42 main-process, 44 renderer), and the user accepted the feature before the 3.2.0 release.
- Implemented host/local-authoritative `settlement.json` and durable `settlement-journal.json` in the Survivors folder. Knowledge and Tenet Knowledge share normalized name/level identity; successful saves register permanent definition snapshots, and existing saved survivors seed the record. Metadata is protected from survivor CRUD. Survivor schema/version and package version are unchanged.
- Added recoverable prepared/committed/completed operations with ordered audit events; interrupted saves are resolved by content digest without replaying stale survivor writes. Failed registration remains pending and returns a visible successful-save warning. Recovery occurs on subsequent writes and settlement reads, not a polling timer.
- Added provider/IPC/LAN settlement lookup and unlock-first picker grouping, including a disabled separator, cross-type deduplication, stored-definition fallback and search. Documentation and manual acceptance checklist: `docs/ai/SETTLEMENT_RECORD.md`.
- User accepted the Showdown refactor manual check ("Manually looks, fine"); Step 9 is complete and the refactor shipped in 3.2.0.

## Current State Snapshot
- Linux x86_64/ARM64 packaging, Asahi acceptance, and tagged publishing are complete for v3.1.0. Release run `33918417489` passed all macOS, Windows, native Linux x64/ARM64, and publication jobs; the public release contains both Linux tarball/DEB/RPM sets and corrected per-architecture checksum manifests. Future Linux artifact names are space-free so GitHub cannot rewrite them after checksum generation. Native x86_64 installed-package validation is intentionally delegated to post-release user feedback; AppImage and Flatpak remain separate experimental formats.
- Project/package version is `3.3.0`. Survivor schema remains strictly version `6`; older/missing/future versions are rejected, not migrated.
- Legacy `config.dataPath`, the `tenetKnowledges` Data Source/UI/fallback, and the survivor `philosophyTenet` field have been removed. Knowledge and Tenet Knowledge templates both require `knowledges`.
- Settlement refresh shows an explicit v3.0.1 campaign-reset/schema-6 error when incompatible survivor files are skipped, directing users to choose a new Survivors folder.
- LAN survivor-data Phases 1–7 are complete and have passed a real-world trial: Settings shows client-enterable LAN Host URLs, can discover LAN hosts automatically, has a manual survivor-folder backup export, rolls failed host starts back to `lanHostEnabled: false`, blocks disconnected client writes, uses structured LAN error messages, and refreshes Settlement from host push events.
- LAN survivor-data live refresh is implemented with Server-Sent Events: LAN Host broadcasts survivor save/delete changes from `/events`, LAN Client subscribes and reconnects the stream, and Settlement refreshes automatically when host survivor data changes.
- LAN offline reads preserve the current view/data and show consistent reconnect guidance; startup continues into a usable app when the configured host is unavailable.
- LAN Settings now has explicit `Start Host`, `Stop Host`, `Connect`, and `Disconnect` actions; `lanClientConnected` persists client disconnect state without clearing the saved host address, and client writes perform a fresh status check before persistence.
- LAN push refresh intentionally targets Settlement; Create/Edit and Showdown retain in-memory work and use explicit refresh actions to avoid silent replacement.
- LAN survivor-data Phase 5 navbar status indicator is implemented: Settings can choose `Local Files`, `LAN Host`, or `LAN Client`; LAN Host can start a main-process HTTP JSON survivor CRUD service when enabled; LAN Client routes survivor calls to the configured host; the header shows compact `Local`/`Hosting`/`Connected`/`Offline`/`Error` status and opens Settings when clicked.
- Survivor IPC routes through `src/survivorProvider.js` for Local Files, LAN Host, and LAN Client modes; the completed design and maintenance boundaries are recorded in the LAN plan and implementation handoff.
- Survivor records now have stable `id` values and save as `{survivor-id}_{name-slug}.json`; legacy name-only files load with deterministic `survivor-legacy-{filename-slug}` ids and migrate on save.
- Survivor records now include `createdAt`; legacy records missing it migrate from `updatedAt`, then `lastUpdated`, then the current timestamp, and later saves preserve it.
- Survivor saves now create best-effort pre-save history snapshots for existing records under `history/survivors/{survivor-id}/` in the configured survivor data folder; first-time creates do not create history, and the main save still proceeds if history writing fails.
- Showdown combat Temp modifiers can be positive or negative; token buckets still clamp at zero and remain temporary/non-persistent.
- Project/package version is now `2.2.9` for the Showdown combat Temp modifier clamp fix.
- Settlement Lifetime Reroll filtering depends on `listPeopleSummaries` including `person.lifetimeReroll`; renderer smoke coverage mirrors the settlement summary shape so future extraction changes should keep that field.
- Project/package version is now `2.2.4` for the Knowledge limit patch release.
- Repo-level `AGENTS.md` has been restored as the primary working brief for future agents, consolidating current UI direction, workflow guardrails, and documentation expectations from the active handoff/context docs.
- `macOS Package` and `Windows Package` workflows are now manual-only (`workflow_dispatch`) so routine `main`/PR changes run validation without duplicate packaging; cross-platform packaging for public binaries is centralized in `Release Publish`.
- Release publish workflow now checks out repo content before artifact downloads in the publish job, preventing downloaded `.dmg`/`.zip`/`.exe` artifacts from being removed before `action-gh-release` uploads them.
- GitHub Releases now publish a consistent custom release body from `.github/release-notes.md`, including explicit macOS first-launch quarantine-removal guidance for unsigned app builds.
- Standalone `macOS Package` CI workflow now includes install retry protection for transient Electron download failures (`npm ci` with retries), matching hardened behavior in release/Windows flows.
- Public-facing repository README is now present, describing product purpose/workflow and pointing users to the latest GitHub Release downloads for Windows, macOS, and Linux.
- Project/package version is now `1.1.1` for the next automated tagged release.
- Release workflow now supports both automatic tag publishing and manual runs with explicit `tag` input, and uses platform-appropriate retry logic for dependency install stability on both macOS and Windows release jobs.
- Windows CI dependency installation now includes retry logic (`npm ci` up to 3 attempts with delay) in both build-only and release-publish workflows to mitigate transient Electron download socket failures on hosted runners.
- Fully automated public release publishing is configured for macOS, Windows, Linux x64, and Linux ARM64 artifacts on `v*` tags; Linux publishes core tarball/DEB/RPM formats plus per-architecture checksums, while AppImage and Flatpak remain separate experimental workflows.
- Repository process guardrails are now in place for safer day-to-day development: `npm run verify`, `npm run preflight`, commit/release checklists, PR template, and a dedicated CI validation workflow for `main`/PRs.
- Repository now includes `package.json` and `package-lock.json`, allowing GitHub Actions `setup-node` npm cache and `npm ci`-based package workflows to resolve dependencies correctly.
- CI packaging workflows now disable auto-publish (`--publish never`) for macOS/Windows/Linux artifact jobs, avoiding `GH_TOKEN` failures when only build artifacts are needed.
- Project/package version is now `1.1.0`, and macOS release artifacts for this version were generated locally in `release/` (`dmg` + `zip`, arm64).
- Header navigation now includes a persistent light/dark theme toggle (right-aligned) that stores preference in `localStorage` and applies app-wide complementary light-theme overrides while preserving the dark-fantasy dark mode as default.
- Light-theme contrast polish now hardens readability for nav active/hover states, status pills, showdown stepper controls, and philosophy/paragraph fields that previously retained dark-surfaces with dark text.
- Settlement slot selector buttons (`1`/`2`) in light mode now have explicit default/hover/active/disabled contrast styling for reliable visibility against the light table background.
- Create/View knowledge upgrade actions now mirror showdown gating visually: row-level `Upgrade` buttons are hidden until `currentObservations >= observationRequirement` and mode is not `MAX LEVEL`, with live visibility updates while editing the row fields.
- Light-theme `Ready to Ponder` pill now uses a stronger success background/border treatment to stand out more clearly from neutral pills.
- Create Survivor knowledge entries now render as distinct card-like areas with stronger inter-row separation, and `Save Template` is grouped directly next to `Remove` in a dedicated row action area.
- Settlement table sorting now keeps descending as the default first sort direction both on initial load and when switching to a different column.
- App now uses a modern dark-fantasy visual system (palette, typography weighting, elevated surfaces, and control treatments) with a dedicated redesign override layer; Showdown/Create receive stronger hierarchy, spacing rhythm, and section readability without functional changes.
- Showdown and Create/View layouts received a UI polish pass (spacing rhythm, grouped section breathing room, denser/cleaner action alignment) without behavior changes.
- Survivor records now persist neurosis template/source label via `philosophyNeurosisName` (optional, backward-compatible), and Showdown displays neurosis as read-only text instead of editable fields.
- Neurosis template loading now reuses the same template modal component as Knowledge templates for visual parity; modal mode switches copy/search/select behavior and hides the scratch action for neurosis loads.
- Neurosis template name fields no longer default from survivor names; they start empty and now require explicit entry on save, with showdown name-input state preserved per slot and auto-filled only when loading a selected template.
- Neurosis template loading now uses a modal picker flow (search + select + apply) in Create/View and Showdown, matching the knowledge-template interaction pattern and removing prompt-based template selection.
- Neurosis template save flows in both Showdown and Create/View now support explicit user-entered template names via visible name inputs (no prompt required).
- Create/View Survivor (including settlement edit flow) now mirrors showdown neurosis handling with Philosophy Neurosis template actions (`Load Template`, `Save as Template`) and proper `philosophyNeurosis` form render/save binding.
- Showdown `Tenet Knowledge` section is now `Tenet Knowledge and Neurosis`, showing editable survivor neurosis plus load/save template actions backed by the configured `neuroses` data source.
- Philosophy Tenet has been removed from the UI and survivor schema; `philosophyTenet` is no longer accepted in survivor JSON.
- Showdown view now includes a `Refresh Survivors` action that reloads selected showdown survivors from persisted settlement data (non-departed sessions only).
- Ending showdown (`Showdown Over`) now clears showdown slot selections and forces explicit reselection of two survivors before opening another showdown.
- Showdown `Knowledge` and `Tenet Knowledge` entries now render with clearer visual separation between multiple rows using divider spacing.
- Tenet Knowledge templates resolve from the same configured `knowledges` source path as Knowledge templates, with no legacy fallback.
- Create/View Survivor now has a dedicated `Back` button above the save button to quickly return to Settlement without making changes.
- Create/Edit Survivor knowledge rows now include an `Upgrade` action that follows showdown upgrade rules (`currentObservations >= observationRequirement`, `nextKnowledgeMode` handling, template-or-blank progression).
- Showdown Fighting Arts, Secret Fighting Arts, and Disorders now display inline markdown preview text beneath each linked entry name (in addition to click-to-open full markdown modal).
- Showdown survivor cards now expose an editable `Alive` boolean toggle (`isAlive`) so death state can be updated during showdown and persisted on showdown save.
- Abilities/Impairments in Create Survivor and Showdown now use commit flow: `Add New` -> editable free text -> `Save/Commit`, then read-only paragraph with `Edit`/`Remove`.
- Survivor schema version `6` is enforced strictly in dataService with no version migration.
- Showdown armor locations now include temporary Light/Heavy checkboxes (Head: Heavy only), stored in showdown memory only (non-persistent).
- Showdown includes a temporary Bleeding Tokens counter in the vitals group (non-persistent).
- Multi-view Electron app is operational (Technical/Create/Settlement/Showdown).
- Showdown session lifecycle supports Depart lock and Showdown Over finalize.
- Knowledge progression fields and upgrade behavior are implemented.
- Knowledge template library is persisted and searchable in picker modal.
- Rename while editing existing survivor removes old record to prevent duplicates.
- Packaging scripts/config are defined via `electron-builder` in `package.json` for macOS, Windows, and Linux targets (including Flatpak).
- Survivor saves now use optimistic concurrency (`revision`, `updatedAt`) and atomic file writes.

## Recent Changes
- 2026-09-06: Reconciled the engineering roadmap after 3.2.1 and documented two candidate, not-yet-selected directions for centrally declared feature capabilities and settlement authority: Option A retains Local Files for survivor features while gating persistent settlement features to Host/Client, and Option B removes Local Files from the main app in favor of a mandatory first-run Host/Client choice and connection gate, with any Local-only continuation treated as a possible separate app or fork; the survivor index remains deferred, and Knowledge predecessor work follows the authority decision and capability foundation; files: `docs/FEATURE_CAPABILITY_AND_SETTLEMENT_AUTHORITY_PLAN.md`, `docs/KNOWLEDGE_PREDECESSOR_LINK_PLAN.md`, `docs/ai/ROADMAP.md`, `docs/ai/MODEL_HANDOFF.md`; verification: documentation-only update, `git diff --check`.
- 2026-09-06: Prepared the 3.2.1 maintenance patch after user-confirmed Windows setup/portable acceptance; removed the obsolete 2026-08-10 engineering checkpoint and completed Electron Builder pickup list from `AGENTS.md`, retaining the Create/View Survivor action-rail treatment as a permanent UI guardrail; synchronized local dependencies to the lockfile; and updated transitive `fast-uri` from 3.1.5 to 3.1.7 after a fresh npm audit identified new high-severity advisories; files: `AGENTS.md`, `package.json`, `package-lock.json`, `CHANGELOG.md`, `.github/release-notes.md`, `docs/ai/MODEL_HANDOFF.md`; verification: `npm run preflight` (246/246 tests passed), production and full `npm audit` (0 vulnerabilities), `git diff --check`.
- 2026-09-05: Completed Showdown Step 7. Moved selection population/assignment/change handlers, open/refresh/depart/completion, session resets, save result handling, and partial-save reconciliation into `rendererShowdownController.js`. Injected selector elements, a live session accessor interface, navigation/UI callbacks, and survivor API services; existing cross-view callers use forwarding functions. Renderer is 5,136 lines; controller is 956 lines and its interaction/lifecycle boundary should be reviewed during Step 8 cleanup. Verification: renderer smoke 41/41, full `npm run verify` 229/229, syntax checks, and `git diff --check` passed. Manual acceptance remains Step 9.
- 2026-09-05: Finished Showdown Step 6 after the earlier delegated-event extraction. Moved ten inline text/card mutation coordination functions into `rendererShowdownController.js`, reading current survivor/modifier/draft objects through `getState()` on each operation; reduced renderer dependency wiring and corrected controller indentation. Shared template/markdown operations and rendering remain injected, and selection/refresh/depart/completion/partial-save lifecycle remains in `renderer.js` for Step 7. Verification: renderer smoke 41/41, full `npm run verify` 229/229, syntax checks, and `git diff --check` passed.
- 2026-09-04: Completed Step 5 of the Showdown renderer decomposition by moving survivor-card `innerHTML` assignment and accordion snapshot/restoration into the public `rendererShowdownView.js` render operation; `renderer.js` still selects and normalizes slots, supplies explicit render inputs, coordinates markdown loading, and owns all delegated events and session lifecycle behavior, and was reduced from 5,913 to 5,881 lines; files: `src/rendererShowdownView.js`, `src/renderer.js`, `docs/ai/ROADMAP.md`, `docs/ai/MODEL_HANDOFF.md`; verification: `node --check src/rendererShowdownView.js src/renderer.js`, `node test/renderer.smoke.test.js` (41/41 passed), `npm run verify` (229/229 passed), `git diff --check`.
- 2026-09-04: Completed Step 4 of the revised Showdown renderer decomposition by renaming the state-only helper to `src/rendererShowdownState.js`, creating `src/rendererShowdownView.js`, loading both before `renderer.js` in the live page and smoke harness, and moving the full survivor-card markup builder into the View module behind explicit card data and formatting/content callbacks; `renderer.js` still owns input normalization, draft sync, DOM assignment, accordion preservation, markdown cache/loading, events, and lifecycle, and was reduced from 6,395 to 5,913 lines; files: `src/rendererShowdownState.js`, `src/rendererShowdownView.js`, `src/renderer.js`, `ui/components/index.html`, `test/renderer.smoke.test.js`, `docs/ai/ROADMAP.md`, `docs/ai/MODEL_HANDOFF.md`; verification: `node --check src/rendererShowdownState.js src/rendererShowdownView.js src/renderer.js test/renderer.smoke.test.js`, `node test/renderer.smoke.test.js` (41/41 passed), `npm run verify` (229/229 passed), `git diff --check`.
- 2026-09-04: Refined the renderer decomposition strategy before Showdown Step 4: future Showdown work now targets three explicit browser-safe modules (`rendererShowdownState.js`, `rendererShowdownView.js`, and `rendererShowdownController.js`) instead of growing a generic helper file; audited the other renderer helpers and found the Knowledge helper cohesive, the current Showdown state helper cohesive but in need of an accurate rename, and the 400-line Settlement helper a moderate Data/View/Controller mini-monolith whose split is intentionally deferred until after Showdown or the next meaningful Settlement change; files: `docs/ai/ROADMAP.md`, `docs/ai/MODEL_HANDOFF.md`; verification: documentation-only update, `git diff --check`.
- 2026-09-04: Completed Step 3 of the Showdown renderer decomposition by moving state-only page stepping, modifier creation/validation/clamping, armor counter and Heavy-to-Light check handling, and inline text-draft sync/edit/update/commit operations into `src/rendererShowdownHelpers.js` behind explicit state arguments; `renderer.js` retains DOM rendering, delegated events, status feedback, markdown loading, and session/save lifecycle ownership, and the roadmap now resumes at Step 4 card-markup extraction; files: `src/rendererShowdownHelpers.js`, `src/renderer.js`, `docs/ai/ROADMAP.md`, `docs/ai/MODEL_HANDOFF.md`; verification: helper operation assertions passed, `node test/renderer.smoke.test.js` (41/41 passed), `npm run verify` (229/229 passed), `git diff --check`.
- 2026-09-04: Completed Step 2 of the Showdown renderer decomposition by adding the browser-safe `src/rendererShowdownHelpers.js` boundary, loading it before `renderer.js` in the live page and smoke harness, and moving only Showdown constants, fresh armor/modifier/page/text-draft state factories, and page-key normalization out of the main renderer; rendering, events, mutations, and lifecycle persistence remain renderer-owned, and the roadmap now resumes at Step 3 pure state operations; files: `src/rendererShowdownHelpers.js`, `src/renderer.js`, `ui/components/index.html`, `test/renderer.smoke.test.js`, `docs/ai/ROADMAP.md`, `docs/ai/MODEL_HANDOFF.md`; verification: `node test/renderer.smoke.test.js` (41/41 passed), `npm run verify` (229/229 passed), `git diff --check`.
- 2026-09-04: Completed Step 1 of the Showdown renderer decomposition on the full-work branch `refactor/showdown-renderer-decomposition` by adding regression coverage for successful completion/reset, pre-depart persisted-data refresh, temporary combat-state reset, inline Abilities/Impairments/Notes persistence, and page/accordion preservation across slot rerenders; enhanced the renderer smoke fake DOM only enough to track rendered Showdown `<details>` state; recorded Step 2 as the exact roadmap resume point (`src/rendererShowdownHelpers.js`, pure helpers only); files: `test/renderer.smoke.test.js`, `docs/ai/ROADMAP.md`, `docs/ai/MODEL_HANDOFF.md`; verification: `node test/renderer.smoke.test.js` (41/41 passed), `npm run verify` (229/229 passed), `git diff --check`.
- 2026-09-04: Updated every GitHub-hosted workflow from Node 20-based action releases to the current Node 24-based majors (`actions/checkout@v7`, `actions/setup-node@v7`, `actions/upload-artifact@v7`, and `actions/download-artifact@v8`), without changing the app's Node `>=22.12.0` compatibility policy; files: `.github/workflows/*.yml`, `CHANGELOG.md`, `docs/ai/MODEL_HANDOFF.md`; verification: all workflow YAML parsed successfully and `npm run verify` passed 225 tests; hosted validation pending.
- 2026-09-04: Prepared v3.1.1 release metadata for the completed LAN reliability roadmap, expanded renderer regression coverage, and stable Linux artifact filenames; files: `package.json`, `package-lock.json`, `CHANGELOG.md`, `.github/release-notes.md`, `docs/ai/MODEL_HANDOFF.md`; verification: `npm run preflight` (225 tests passed).
- 2026-09-04: Completed the LAN Phase 7 closeout by making LAN-client survivor read failures non-fatal during startup, adding consistent in-view reconnect/retry guidance while preserving Settlement, editor, and Showdown state, documenting Settlement-only push refresh as an intentional data-safety boundary, and reconciling the LAN/main roadmap statuses; files: `src/renderer.js`, `test/renderer.smoke.test.js`, `docs/ai/LAN_SURVIVOR_PLAN.md`, `docs/ai/LAN_IMPLEMENTATION_HANDOFF.md`, `docs/ai/ROADMAP.md`, `docs/ai/MODEL_HANDOFF.md`; verification: `npm run verify` (225 tests passed).
- 2026-08-10: Flattened the Create/View Survivor sticky action rail by removing its theme-specific panel background, border, padding, blur, and shadow so Back/Create-or-Save/Edit Default Template read as standalone actions instead of buttons attached to a mismatched floating bubble; sticky positioning and button styling remain unchanged across Dark, Light, Zen Day, and Zen Night; files: `ui/components/styles/theme-dark.css`, `ui/components/styles/theme-light.css`, `ui/components/styles/theme-zen.css`, `docs/ai/MODEL_HANDOFF.md`; verification: `npm run verify`, `git diff --check`.
- 2026-08-10: Updated Electron from 40.6.0 to 41.10.4 while keeping electron-builder pinned at 24.13.3 for isolated regression testing; the Electron downloader stack moved from `@electron/get` 2 to 5 and removed obsolete transitive packages, and Electron is no longer present in the npm advisory report; files: `package.json`, `package-lock.json`, `docs/ai/MODEL_HANDOFF.md`; verification: Electron runtime and packaged runtime both report 41.10.4, `npm run verify` (220 tests passed), `npm audit --omit=dev` (0 production-dependency vulnerabilities), full audit reduced to 13 electron-builder/toolchain findings, `npm run package:dir` (macOS arm64 succeeded; unsigned as expected), `git diff --check`; follow-up: validate Windows installer/portable packaging in CI before release.
- 2026-08-10: Updated low-risk runtime dependencies to `ajv` 8.20.0 and `markdown-it` 14.3.0, including fixed transitive `fast-uri` 3.1.5 and `linkify-it` 5.0.2 releases; production dependency audit now reports zero vulnerabilities while Electron and electron-builder remain intentionally unchanged for separate validation; files: `package.json`, `package-lock.json`, `docs/ai/MODEL_HANDOFF.md`; verification: `npm run verify` (220 tests passed), `npm audit --omit=dev` (0 vulnerabilities), `git diff --check`.
- 2026-08-10: Recorded the successful real-world LAN Host / LAN Client trial and archived the executed v3.0.1 backward-compatibility removal playbook under `docs/ai/archive/`; verification: documentation-only follow-up after the completed release preflight.
- 2026-08-10: Prepared `v3.0.1` as an explicit breaking campaign-reset release: bumped survivor schema to strict v6, removed legacy `dataPath` config loading, removed the deprecated `tenetKnowledges` source/UI/fallback and `philosophyTenet` field, added an explicit in-app reset warning for skipped incompatible files, updated tests, and added prominent backup/reset warnings to release-facing docs; files: `package.json`, `package-lock.json`, `src/dataService.js`, `src/main.js`, `src/renderer.js`, `src/validation/person.schema.json`, `ui/components/index.html`, `test/`, `CHANGELOG.md`, `README.md`, `.github/release-notes.md`, `docs/ai/`; verification: `node --check src/main.js src/preload.js src/dataService.js src/renderer.js`, `npm test`, `npm run verify`, `git diff --check`.
- 2026-05-12: Prepared the `v3.0.0` release metadata and GitHub release notes for the LAN survivor-data release, including the version bump, changelog entry, release notes covering Host/Client modes, auto discovery, live Settlement refresh, reconnect/status hardening, explicit LAN actions, and backup export, plus a CI hardening fix so the best-effort UDP discovery socket is unref'd and cannot hold test processes open; files: `package.json`, `package-lock.json`, `CHANGELOG.md`, `.github/release-notes.md`, `src/main.js`, `docs/ai/MODEL_HANDOFF.md`; verification: `node --check src/main.js src/preload.js src/dataService.js src/lanSurvivorHost.js src/survivorProvider.js src/rendererKnowledgeTemplateHelpers.js src/rendererSettlementHelpers.js src/renderer.js test/main.handlers.test.js test/renderer.smoke.test.js`, `git diff --check`, `npm test`, `npm run verify`.
- 2026-05-12: Added automatic LAN discovery using best-effort UDP host advertisements, with LAN Client Settings controls to scan discovered hosts and fill the selected host address/port automatically while preserving manual host entry as fallback; files: `src/main.js`, `src/preload.js`, `src/renderer.js`, `ui/components/index.html`, `test/main.handlers.test.js`, `test/renderer.smoke.test.js`, `docs/ai/LAN_IMPLEMENTATION_HANDOFF.md`, `docs/ai/LAN_SURVIVOR_PLAN.md`, `docs/ai/PROJECT_CONTEXT.md`, `docs/ai/MODEL_HANDOFF.md`; verification: `node --check src/main.js src/preload.js src/dataService.js src/lanSurvivorHost.js src/survivorProvider.js src/rendererKnowledgeTemplateHelpers.js src/rendererSettlementHelpers.js src/renderer.js test/main.handlers.test.js test/renderer.smoke.test.js`, `npm test`.
- 2026-05-12: Added final real-world LAN readiness hardening by showing LAN Host URLs from local IPv4 addresses in Settings, adding a manual `Export Backup` action for the configured survivor folder, rolling back `lanHostEnabled` when host startup fails, and reverting LAN Settings controls after save/start failures; files: `src/main.js`, `src/preload.js`, `src/renderer.js`, `ui/components/index.html`, `ui/components/styles/base.css`, `test/main.handlers.test.js`, `test/renderer.smoke.test.js`, `docs/ai/LAN_IMPLEMENTATION_HANDOFF.md`, `docs/ai/LAN_SURVIVOR_PLAN.md`, `docs/ai/PROJECT_CONTEXT.md`, `docs/ai/MODEL_HANDOFF.md`; verification: `node --check src/main.js src/preload.js src/dataService.js src/lanSurvivorHost.js src/survivorProvider.js src/rendererKnowledgeTemplateHelpers.js src/rendererSettlementHelpers.js src/renderer.js test/main.handlers.test.js test/renderer.smoke.test.js`, `npm test`.
- 2026-05-12: Completed a LAN reliability pass after SSE live refresh, adding generation guards so stale event-stream callbacks cannot affect a newer connection, handling SSE response close events, sending host keepalive comments, cleaning up host start failures so retry works, and mapping delete failures through the same structured survivor error path as saves; files: `src/main.js`, `src/lanSurvivorHost.js`, `src/renderer.js`, `test/lanSurvivorHost.test.js`, `test/main.handlers.test.js`, `test/renderer.smoke.test.js`, `docs/ai/LAN_IMPLEMENTATION_HANDOFF.md`, `docs/ai/MODEL_HANDOFF.md`; verification: `node --check src/main.js src/preload.js src/dataService.js src/lanSurvivorHost.js src/survivorProvider.js src/rendererKnowledgeTemplateHelpers.js src/rendererSettlementHelpers.js src/renderer.js test/lanSurvivorHost.test.js test/main.handlers.test.js test/renderer.smoke.test.js`, `node --test test/lanSurvivorHost.test.js test/main.handlers.test.js test/renderer.smoke.test.js`, `npm test`.
- 2026-05-12: Added LAN push refresh via Server-Sent Events by exposing host `/events`, broadcasting survivor-data changes after successful host saves/deletes, subscribing from the LAN Client main process with Auto Reconnect support, forwarding status/data change notifications through preload, and refreshing Settlement automatically when pushed host changes arrive; files: `src/lanSurvivorHost.js`, `src/main.js`, `src/preload.js`, `src/renderer.js`, `test/lanSurvivorHost.test.js`, `test/renderer.smoke.test.js`, `docs/ai/LAN_IMPLEMENTATION_HANDOFF.md`, `docs/ai/LAN_SURVIVOR_PLAN.md`, `docs/ai/PROJECT_CONTEXT.md`, `docs/ai/MODEL_HANDOFF.md`; verification: `node --check src/main.js src/preload.js src/dataService.js src/lanSurvivorHost.js src/survivorProvider.js src/rendererKnowledgeTemplateHelpers.js src/rendererSettlementHelpers.js src/renderer.js`, `node --test test/lanSurvivorHost.test.js test/renderer.smoke.test.js`, `npm test`.
- 2026-05-12: Hardened LAN reliability messaging by marking LAN client unreachable-host failures as `host-unavailable`, surfacing `Reconnecting` status with a shorter Auto Reconnect retry interval while offline/error, mapping LAN client save failures into distinct validation/stale-conflict/host-unavailable/server-error messages, and refining showdown partial-save failure copy; files: `src/survivorProvider.js`, `src/main.js`, `src/renderer.js`, `ui/components/styles/base.css`, `test/survivorProvider.test.js`, `test/renderer.smoke.test.js`, `docs/ai/LAN_IMPLEMENTATION_HANDOFF.md`, `docs/ai/LAN_SURVIVOR_PLAN.md`, `docs/ai/PROJECT_CONTEXT.md`, `docs/ai/MODEL_HANDOFF.md`; verification: `node --check src/main.js src/survivorProvider.js src/renderer.js`, `node --test test/survivorProvider.test.js test/renderer.smoke.test.js test/main.handlers.test.js`.
- 2026-05-12: Added explicit LAN Settings actions for `Start Host`, `Stop Host`, `Connect`, and `Disconnect`, introduced backward-compatible persisted `lanClientConnected` state, and hardened client writes with a fresh pre-save status check before survivor persistence; files: `src/dataService.js`, `src/main.js`, `src/survivorProvider.js`, `src/renderer.js`, `ui/components/index.html`, `ui/components/styles/base.css`, `test/dataService.test.js`, `test/dataService.util.test.js`, `test/renderer.smoke.test.js`, `docs/ai/LAN_IMPLEMENTATION_HANDOFF.md`, `docs/ai/LAN_SURVIVOR_PLAN.md`, `docs/ai/PROJECT_CONTEXT.md`, `docs/ai/MODEL_HANDOFF.md`; verification: `node --check src/main.js src/dataService.js src/survivorProvider.js src/renderer.js`, `node --test test/dataService.util.test.js test/dataService.test.js test/survivorProvider.test.js test/renderer.smoke.test.js`.
- 2026-05-12: Started v3 LAN survivor-data Phase 6 recovery behavior by refreshing the navbar LAN status after survivor list/load/save/delete operations in client mode, disabling client-mode survivor write controls when the latest status is `Offline` or `Error`, and guarding save/delete/bulk/showdown write flows with recovery messaging; files: `src/renderer.js`, `test/renderer.smoke.test.js`, `docs/ai/LAN_IMPLEMENTATION_HANDOFF.md`, `docs/ai/LAN_SURVIVOR_PLAN.md`, `docs/ai/MODEL_HANDOFF.md`; verification: `node --check src/renderer.js`, `node --test test/renderer.smoke.test.js`.
- 2026-05-11: Implemented v3 LAN survivor-data Phase 5 navbar status indicator by adding a compact status-only header button, exposing `getLanConnectionStatus` through main/preload, reporting local/host/client states including a client `/health` check with timeout, routing indicator clicks to Settings, and adding main/renderer coverage; files: `ui/components/index.html`, `ui/components/styles/base.css`, `src/main.js`, `src/preload.js`, `src/renderer.js`, `test/main.handlers.test.js`, `test/renderer.smoke.test.js`, `docs/ai/LAN_IMPLEMENTATION_HANDOFF.md`, `docs/ai/LAN_SURVIVOR_PLAN.md`, `docs/ai/PROJECT_CONTEXT.md`, `docs/ai/MODEL_HANDOFF.md`; verification: `node --check src/main.js src/preload.js src/renderer.js`, `node --test test/main.handlers.test.js test/renderer.smoke.test.js`.
- 2026-05-11: Implemented v3 LAN survivor-data Phase 4 client routing by adding a LAN Client survivor provider that calls the host HTTP API, preserving the existing renderer `window.api` contract, mapping host conflict/validation responses back to existing data-service error classes, awaiting async provider calls in survivor IPC handlers, and allowing LAN Client workflows with no local Survivors folder when a host address is configured; files: `src/survivorProvider.js`, `src/main.js`, `src/renderer.js`, `test/survivorProvider.test.js`, `test/renderer.smoke.test.js`, `docs/ai/LAN_IMPLEMENTATION_HANDOFF.md`, `docs/ai/LAN_SURVIVOR_PLAN.md`, `docs/ai/PROJECT_CONTEXT.md`, `docs/ai/MODEL_HANDOFF.md`; verification: `node --check src/main.js src/survivorProvider.js src/renderer.js`, `node --test test/survivorProvider.test.js test/renderer.smoke.test.js test/main.handlers.test.js`.
- 2026-05-11: Continued v3 LAN survivor-data work with Phase 2 Settings controls and the Phase 3 LAN Host HTTP API foundation: added compact Survivor Data settings for Local Files / LAN Host / LAN Client, persisted all LAN controls from the renderer, hid the Survivors folder picker in client mode, made LAN Host use local `dataService` as authoritative storage, added `src/lanSurvivorHost.js` for health and survivor CRUD HTTP JSON endpoints, and wired main-process host start/stop to saved `lan-host` + `lanHostEnabled` settings; files: `ui/components/index.html`, `ui/components/styles/base.css`, `src/renderer.js`, `src/main.js`, `src/survivorProvider.js`, `src/lanSurvivorHost.js`, `test/renderer.smoke.test.js`, `test/survivorProvider.test.js`, `test/lanSurvivorHost.test.js`, `test/compile.test.js`, `package.json`, `docs/ai/LAN_IMPLEMENTATION_HANDOFF.md`, `docs/ai/LAN_SURVIVOR_PLAN.md`, `docs/ai/PROJECT_CONTEXT.md`, `docs/ai/MODEL_HANDOFF.md`; verification: `node --check src/main.js src/lanSurvivorHost.js src/survivorProvider.js src/renderer.js`, `node --test test/survivorProvider.test.js test/lanSurvivorHost.test.js test/main.handlers.test.js test/renderer.smoke.test.js`.
- 2026-05-11: Started the v3 LAN survivor-data foundation by adding a survivor-provider abstraction (`src/survivorProvider.js`), routing survivor IPC through it in local mode, persisting future LAN settings fields, preserving those fields from renderer settings saves, and adding a dedicated LAN implementation handoff file; files: `src/survivorProvider.js`, `src/main.js`, `src/dataService.js`, `src/renderer.js`, `test/survivorProvider.test.js`, `test/compile.test.js`, `test/dataService.test.js`, `test/dataService.util.test.js`, `test/renderer.smoke.test.js`, `package.json`, `docs/ai/LAN_IMPLEMENTATION_HANDOFF.md`, `docs/ai/LAN_SURVIVOR_PLAN.md`, `docs/ai/PROJECT_CONTEXT.md`, `docs/ai/MODEL_HANDOFF.md`; verification: `node --check src/main.js src/preload.js src/dataService.js src/survivorProvider.js src/rendererKnowledgeTemplateHelpers.js src/rendererSettlementHelpers.js src/renderer.js`, `node --test test/survivorProvider.test.js test/dataService.test.js test/dataService.util.test.js test/main.handlers.test.js test/main.ipc.test.js test/renderer.smoke.test.js`, `npm test`.
- 2026-05-11: Added persistent survivor `createdAt` timestamps, bumped survivor schema to v5, and migrated legacy records by deriving missing `createdAt` from `updatedAt`/`lastUpdated` while preserving it across future saves; files: `src/dataService.js`, `src/validation/person.schema.json`, `test/dataService.schema.test.js`, `test/dataService.test.js`, `test/dataService.util.test.js`, `test/dataService.edge.test.js`, `test/renderer.smoke.test.js`, `AGENTS.md`, `docs/ai/PROJECT_CONTEXT.md`, `docs/ai/MODEL_HANDOFF.md`; verification: `node --check src/main.js src/preload.js src/dataService.js src/rendererKnowledgeTemplateHelpers.js src/rendererSettlementHelpers.js src/renderer.js`, `node --test test/dataService.test.js test/dataService.schema.test.js test/dataService.util.test.js test/dataService.edge.test.js test/renderer.smoke.test.js`, `npm test`.
- 2026-05-11: Added stable survivor IDs and ID-prefixed survivor filenames (`{survivor-id}_{name-slug}.json`) so display-name changes preserve identity; legacy name-only files receive deterministic `survivor-legacy-{filename-slug}` ids on load and migrate on save; `savePerson` can discover an existing file by id when no `expectedFileName` is passed, which covers raw/technical rename saves; files: `src/dataService.js`, `src/validation/person.schema.json`, `src/renderer.js`, `test/dataService.test.js`, `test/dataService.schema.test.js`, `test/dataService.util.test.js`, `test/dataService.edge.test.js`, `test/dataService.filesystem.test.js`, `test/renderer.smoke.test.js`, `AGENTS.md`, `docs/ai/PROJECT_CONTEXT.md`, `docs/ai/MODEL_HANDOFF.md`; verification: `node --check src/main.js src/preload.js src/dataService.js src/rendererKnowledgeTemplateHelpers.js src/rendererSettlementHelpers.js src/renderer.js`, `node --test test/dataService.test.js test/dataService.schema.test.js test/dataService.util.test.js test/dataService.edge.test.js`, `node --test test/renderer.smoke.test.js`, `npm test`.
- 2026-05-11: Added app-managed survivor history snapshots for existing survivor saves, including bulk/showdown/edit save paths and rename source tracking via `expectedFileName`; history records wrap the previous survivor JSON with metadata in `history/survivors/{survivor-id}/`; files: `src/dataService.js`, `src/renderer.js`, `test/dataService.test.js`, `docs/ai/PROJECT_CONTEXT.md`, `docs/ai/MODEL_HANDOFF.md`; verification: `node --check src/main.js src/preload.js src/dataService.js src/rendererKnowledgeTemplateHelpers.js src/rendererSettlementHelpers.js src/renderer.js`, `node --test test/dataService.test.js`, `npm test`.
- 2026-05-04: Prepared `v2.2.9` release metadata and patch notes for the Showdown combat Temp modifier clamp fix; files: `package.json`, `package-lock.json`, `CHANGELOG.md`, `.github/release-notes.md`, `docs/ai/MODEL_HANDOFF.md`; verification: `node --check src/main.js src/preload.js src/dataService.js src/rendererKnowledgeTemplateHelpers.js src/rendererSettlementHelpers.js src/renderer.js`, `npm test`, `npm run verify`.
- 2026-05-04: Fixed Showdown combat Temp modifiers so they can step below zero while keeping `Tokens (+)` / `Tokens (-)` clamped at zero; added renderer smoke coverage for negative Temp with token clamping; files: `src/renderer.js`, `test/renderer.smoke.test.js`, `docs/ai/MODEL_HANDOFF.md`; verification: `node --check src/main.js src/preload.js src/dataService.js src/rendererKnowledgeTemplateHelpers.js src/rendererSettlementHelpers.js src/renderer.js`, `node --test test/renderer.smoke.test.js`, `npm test`.
- 2026-05-02: Added `Lumi` to Settlement Bulk Updates with nonnegative clamping so it can be applied to all living survivors alongside the existing stat fields; files: `src/renderer.js`, `test/renderer.smoke.test.js`, `docs/ai/MODEL_HANDOFF.md`; verification: `node --check src/main.js src/preload.js src/dataService.js src/rendererKnowledgeTemplateHelpers.js src/rendererSettlementHelpers.js src/renderer.js`, `node --test test/renderer.smoke.test.js`, `npm test`.
- 2026-05-02: Prepared `v2.2.8` release metadata and patch notes for the Settlement Bulk Updates `Lumi` field addition; files: `package.json`, `package-lock.json`, `CHANGELOG.md`, `.github/release-notes.md`, `docs/ai/MODEL_HANDOFF.md`; verification: `node --check src/main.js src/preload.js src/dataService.js src/rendererKnowledgeTemplateHelpers.js src/rendererSettlementHelpers.js src/renderer.js`, `npm test`.
- 2026-05-02: Fixed Settlement `Stats Total` so it sums only Movement, Speed, Accuracy, Strength, Luck, and Evasion; Courage and Understanding remain visible/sortable on their own but no longer inflate the total column; files: `src/dataService.js`, `src/renderer.js`, `test/dataService.test.js`, `test/renderer.smoke.test.js`, `docs/ai/MODEL_HANDOFF.md`; verification: `node --check src/main.js src/preload.js src/dataService.js src/rendererKnowledgeTemplateHelpers.js src/rendererSettlementHelpers.js src/renderer.js`, `node --test test/dataService.test.js test/renderer.smoke.test.js`, `npm test`.
- 2026-05-02: Prepared `v2.2.7` release metadata and patch notes for the Settlement `Stats Total` calculation fix; files: `package.json`, `package-lock.json`, `CHANGELOG.md`, `.github/release-notes.md`, `docs/ai/MODEL_HANDOFF.md`; verification: `node --check src/main.js src/preload.js src/dataService.js src/rendererKnowledgeTemplateHelpers.js src/rendererSettlementHelpers.js src/renderer.js`, `npm test`.
- 2026-04-30: Investigated Windows white-screen startup regression reported between 2.2.4 and 2.2.5; app source code was unchanged, while Electron/electron-builder/AJV had been refreshed and Windows `nsis`/`portable` builds shared the same `.exe` artifact name. Prepared 2.2.6 by pinning Electron/electron-builder/AJV back to known-good 2.2.4 versions and adding distinct `setup` / `portable` Windows artifact names; files: `package.json`, `package-lock.json`, `CHANGELOG.md`, `.github/release-notes.md`, `docs/ai/MODEL_HANDOFF.md`; verification: `node --check src/main.js src/preload.js src/dataService.js src/rendererKnowledgeTemplateHelpers.js src/rendererSettlementHelpers.js src/renderer.js`, `npm test`, `npm run package:win -- --x64 --publish never`.
- 2026-04-26: Prepared `v2.2.4` release metadata and patch notes for the Knowledge limit increase, including the version bump, changelog entry, and refreshed GitHub release notes; files: `package.json`, `package-lock.json`, `CHANGELOG.md`, `.github/release-notes.md`, `docs/ai/MODEL_HANDOFF.md`; verification: `npm run verify`.
- 2026-04-26: Raised the survivor Knowledge entry limit from 2 to 5 across schema validation and renderer add/template insertion guards; updated filesystem validation coverage and canonical project context; files: `src/validation/person.schema.json`, `src/renderer.js`, `test/dataService.filesystem.test.js`, `docs/ai/PROJECT_CONTEXT.md`, `docs/ai/MODEL_HANDOFF.md`; verification: `node --check src/main.js src/preload.js src/dataService.js src/renderer.js`, `node --test test/dataService.filesystem.test.js`, `npm test`.
- 2026-04-23: Fixed Settlement `Lifetime Reroll` extra filter by adding `person.lifetimeReroll` to the settlement-safe summary payload and updated data-service/renderer smoke coverage so the filter stays wired to the survivor checkbox-backed field; files: `src/dataService.js`, `test/dataService.test.js`, `test/renderer.smoke.test.js`, `docs/ai/MODEL_HANDOFF.md`; verification: `node --check src/main.js src/preload.js src/dataService.js src/rendererSettlementHelpers.js src/renderer.js`, `node --test test/dataService.test.js test/renderer.smoke.test.js`, `npm test`.
- 2026-04-23: Prepared `v2.2.3` release metadata and brief patch notes for the Settlement Lifetime Reroll filter fix and tracked `.DS_Store` cleanup; files: `package.json`, `package-lock.json`, `CHANGELOG.md`, `.github/release-notes.md`, `docs/ai/MODEL_HANDOFF.md`, `release/mac-arm64/.DS_Store`; verification: `npm run verify`.
- 2026-04-16: Prepared `v2.0.0` release metadata and release notes to package the current UI refresh as a major version, including the new `Zen Day` / `Zen Night` themes, flattened Settlement shell, Showdown shell/layout cleanup, and restored knowledge color separation; files: `package.json`, `package-lock.json`, `CHANGELOG.md`, `.github/release-notes.md`, `docs/ai/MODEL_HANDOFF.md`; verification: `node --check src/main.js src/preload.js src/dataService.js src/renderer.js`, `npm test`.
- 2026-04-16: Increased the horizontal gap between the two Showdown survivor shells so the paired columns read as more clearly separated working areas without changing their internal layout or mobile stacking behavior; files: `ui/components/styles/showdown.css`, `docs/ai/MODEL_HANDOFF.md`; verification: `node --check src/main.js src/preload.js src/dataService.js src/renderer.js`, `npm test`.
- 2026-04-16: Removed the outer Settlement view shell by dropping the shared `panel` wrapper from the main settlement container, keeping the table/filer sub-structures intact while flattening the page-level chrome; files: `ui/components/index.html`, `docs/ai/MODEL_HANDOFF.md`; verification: `node --check src/main.js src/preload.js src/dataService.js src/renderer.js`, `npm test`.
- 2026-04-16: Restored stronger internal color separation within Showdown knowledge entries by bringing back tinted `Observation`, `Rules`, and meta blocks with clearer section fills, outlines, and label contrast across dark, light, and Zen themes; files: `ui/components/styles/showdown.css`, `ui/components/styles/theme-dark.css`, `ui/components/styles/theme-light.css`, `ui/components/styles/theme-zen.css`, `docs/ai/MODEL_HANDOFF.md`; verification: `node --check src/main.js src/preload.js src/dataService.js src/renderer.js`, `npm test`.
- 2026-04-16: Removed the experimental Showdown swipe / wheel paging behavior entirely, returning section navigation to the explicit per-survivor page buttons only; files: `src/renderer.js`, `docs/ai/MODEL_HANDOFF.md`; verification: `node --check src/main.js src/preload.js src/dataService.js src/renderer.js`, `npm test`.
- 2026-04-16: Relaxed the restored Showdown survivor shells by increasing their internal padding and slightly widening the overall Showdown canvas while narrowing the inter-card gap, so each survivor card feels roomier and gains a bit more working width without changing behavior; files: `ui/components/styles/showdown.css`, `docs/ai/MODEL_HANDOFF.md`; verification: `node --check src/main.js src/preload.js src/dataService.js src/renderer.js`, `npm test`.
- 2026-04-16: Reverted the experimental Showdown lane treatment and restored rounded outer survivor shells by reapplying the shared `panel` wrapper to both survivor columns and removing the center-divider / surface-wash separation styling; files: `ui/components/index.html`, `ui/components/styles/showdown.css`, `docs/ai/MODEL_HANDOFF.md`; verification: `node --check src/main.js src/preload.js src/dataService.js src/renderer.js`, `npm test`.
- 2026-04-16: Reintroduced survivor separation in Showdown without restoring boxed outer shells by turning each survivor column into a softer lane: widened the inter-column gutter, added a subtle center divider, and applied a faint theme-aware surface wash to each column while keeping borders, shadow, and card chrome off the outer wrapper; files: `ui/components/styles/showdown.css`, `docs/ai/MODEL_HANDOFF.md`; verification: `node --check src/main.js src/preload.js src/dataService.js src/renderer.js`, `npm test`.
- 2026-04-16: Removed the outer survivor card shell from Showdown by dropping the shared `panel` wrapper on the two survivor columns, so the view now reads as two cleaner working surfaces with only the meaningful inner section blocks retained; files: `ui/components/index.html`, `docs/ai/MODEL_HANDOFF.md`; verification: `node --check src/main.js src/preload.js src/dataService.js src/renderer.js`, `npm test`.
- 2026-04-16: Moved Showdown session actions into the main header as a Showdown-only nav group, removing the separate floating `Depart` / `Refresh Survivors` control strip from the view body while preserving the existing `End Showdown` flow in the same header location; files: `ui/components/index.html`, `ui/components/styles/base.css`, `ui/components/styles/showdown.css`, `docs/ai/MODEL_HANDOFF.md`; verification: `node --check src/main.js src/preload.js src/dataService.js src/renderer.js`, `npm test`.
- 2026-04-16: Flattened repeated Showdown/Create inner container chrome to reduce the boxed-in, nested feel: removed panel-style wrappers from identity bars, vitals/armor/stats steppers, array/detail shells, and Create knowledge rows, replacing them with simpler spacing and dividers so sections read as one continuous working surface instead of cards-inside-cards; files: `ui/components/styles/showdown.css`, `ui/components/styles/base.css`, `ui/components/styles/theme-dark.css`, `ui/components/styles/theme-light.css`, `ui/components/styles/theme-zen.css`, `docs/ai/MODEL_HANDOFF.md`; verification: `node --check src/main.js src/preload.js src/dataService.js src/renderer.js`, `npm test`.
- 2026-04-16: Fixed Zen theme parity gaps after the initial theme rollout by overriding inherited dark-only surfaces that remained in Settlement/Create/Modal flows, including the Settlement table body/header, Create knowledge cards, and modal backdrop so `Zen Day` no longer renders dark utility blocks against its light palette; files: `ui/components/styles/theme-zen.css`, `docs/ai/MODEL_HANDOFF.md`; verification: `node --check src/main.js src/preload.js src/dataService.js src/renderer.js`, `npm test`.
- 2026-04-16: Added two new built-in themes, `Zen Day` and `Zen Night`, to the header theme selector with persisted preference support and a dedicated Zen palette layer built around warm shoji-paper neutrals, bamboo greens, and subdued wood/clay accents across shared chrome, forms, Settlement, and Showdown surfaces; files: `ui/components/index.html`, `src/renderer.js`, `ui/components/styles/theme-zen.css`, `test/renderer.smoke.test.js`, `docs/ai/MODEL_HANDOFF.md`; verification: `node --check src/main.js src/preload.js src/dataService.js src/renderer.js`, `npm test`.
- 2026-04-16: Reoriented current UI cleanup toward a stricter utility-first visual direction: reduce nested card chrome, keep layout positions familiar, and avoid defaulting to overly rounded/pill-heavy controls; squarer settings/utility surfaces are preferred where cleaner; no `AGENTS.md` file exists in this workspace, so this guidance is recorded in `docs/ai/PROJECT_CONTEXT.md` and `docs/ai/MODEL_HANDOFF.md`.
- 2026-04-16: Prepared `v1.5.6` release metadata and patch notes; fixed legacy survivor-load compatibility for knowledge entries carrying unsupported extra metadata like `familyID`, added a bulk `All +/-` armor control in Showdown, simplified its header styling, fixed its light-theme button treatment, added a `Heavy` insanity toggle to the vitals card, and combined weapon proficiency type/rank into a tighter shared Showdown control; files: `src/dataService.js`, `src/renderer.js`, `test/dataService.schema.test.js`, `ui/components/styles/showdown.css`, `ui/components/styles/theme-light.css`, `package.json`, `package-lock.json`, `.github/release-notes.md`, `docs/ai/MODEL_HANDOFF.md`; verification: `node --test test/dataService.schema.test.js`, `node --test test/dataService.test.js test/dataService.filesystem.test.js`, `node --check src/renderer.js`, `npm run verify`.
- 2026-04-07: Prepared `v1.5.3` release metadata and patch notes; Showdown now includes `Lifetime Reroll`, uses a cleaner `Depart` -> `End Showdown` control flow without the departed pill, Bulk Updates support multiple simultaneous living-survivor stat changes and reset to a default single row after apply, and the Showdown disorder add-picker filter field now renders correctly in light theme; files: `src/renderer.js`, `ui/components/index.html`, `ui/components/styles/base.css`, `ui/components/styles/theme-light.css`, `package.json`, `package-lock.json`, `.github/release-notes.md`, `docs/ai/PROJECT_CONTEXT.md`, `docs/ai/MODEL_HANDOFF.md`; verification: `node --check src/renderer.js`.
- 2026-03-29: Prepared `v1.5.1` release metadata and patch notes, swapped Showdown armor display order to `Head, Arms, Body, Waist, Legs`, and tightened light-theme dropdown/modal field styling so knowledge-template popup controls no longer retain dark-looking surfaces in light mode; files: `src/renderer.js`, `ui/components/styles/theme-light.css`, `package.json`, `package-lock.json`, `.github/release-notes.md`, `docs/ai/MODEL_HANDOFF.md`; verification: `npm test`.
- 2026-03-07: Temporarily decoupled Linux from automated tagged releases so Windows/macOS deployment can continue while Linux packaging is stabilized: removed Linux build/download steps from `.github/workflows/release-publish.yml` (now macOS + Windows only), kept Linux workflows manual, and updated public release docs/checklists/context accordingly; files: `.github/workflows/release-publish.yml`, `README.md`, `RELEASE_CHECKLIST.md`, `.github/release-notes.md`, `docs/ai/PROJECT_CONTEXT.md`, `docs/ai/MODEL_HANDOFF.md`; verification: `npm run verify`.
- 2026-03-07: Hardened Flatpak packaging config by setting explicit Flatpak base app fields (`base: org.electronjs.Electron2.BaseApp`, `baseVersion: 24.08`) and preinstalling that BaseApp in Linux CI Flatpak setup; also set a non-empty package description for cleaner builder metadata; files: `package.json`, `.github/workflows/linux-package.yml`, `.github/workflows/release-publish.yml`, `docs/ai/PROJECT_CONTEXT.md`, `docs/ai/MODEL_HANDOFF.md`; verification: `npm run verify`.
- 2026-03-07: Fixed Linux CI Flatpak remote permission failure (`ConfigureRemote not allowed for user`) by switching Flatpak runtime setup commands to system scope with `sudo flatpak remote-add/install` in both Linux workflows; files: `.github/workflows/linux-package.yml`, `.github/workflows/release-publish.yml`, `docs/ai/PROJECT_CONTEXT.md`, `docs/ai/MODEL_HANDOFF.md`; verification: `npm run verify`.
- 2026-03-07: Fixed Linux Flatpak CI build prerequisite setup by adding a `Configure Flatpak Runtime` step in Linux workflows (`flatpak remote-add flathub` + install `org.freedesktop.Platform//24.08` and `org.freedesktop.Sdk//24.08`) before packaging, to address release job flatpak build failures on fresh runners; files: `.github/workflows/linux-package.yml`, `.github/workflows/release-publish.yml`, `docs/ai/PROJECT_CONTEXT.md`, `docs/ai/MODEL_HANDOFF.md`; verification: `npm run verify`.
- 2026-03-07: Added Linux Flatpak packaging support (`flatpak` target/config + Linux package script update), installed Flatpak tooling in Linux CI, and introduced explicit post-build Linux artifact smoke checks (asserting `.flatpak`, `.AppImage`, `.deb`, `.rpm`, `.tar.gz`) before artifact upload/publish; files: `package.json`, `.github/workflows/linux-package.yml`, `.github/workflows/release-publish.yml`, `README.md`, `.github/release-notes.md`, `RELEASE_CHECKLIST.md`, `docs/ai/PROJECT_CONTEXT.md`, `docs/ai/MODEL_HANDOFF.md`; verification: `npm run verify`.
- 2026-03-07: Expanded packaging/release coverage to Linux by adding `package:linux` + Linux electron-builder targets (`AppImage`, `deb`, `rpm`, `tar.gz`), introducing a manual `Linux Package` workflow, extending `Release Publish` to build/download/publish Linux artifacts, and updating README/release docs/checklists for Linux install guidance; files: `package.json`, `.github/workflows/linux-package.yml`, `.github/workflows/release-publish.yml`, `README.md`, `.github/release-notes.md`, `RELEASE_CHECKLIST.md`, `docs/ai/PROJECT_CONTEXT.md`, `docs/ai/MODEL_HANDOFF.md`; verification: `npm run verify`.
- 2026-03-01: Removed automatic packaging from day-to-day CI by changing `.github/workflows/macos-package.yml` and `.github/workflows/windows-package.yml` triggers to `workflow_dispatch` only; this avoids duplicate builds on `main`/PR while keeping packaging available manually and via `release-publish`; files: `.github/workflows/macos-package.yml`, `.github/workflows/windows-package.yml`; verification: `node --check src/main.js src/preload.js src/dataService.js src/renderer.js`, `npm test`.
- 2026-03-01: Fixed `release-publish` artifact upload regression by moving `actions/checkout` to the start of the `publish-release` job (before `download-artifact`), so release binaries are preserved and attached instead of publishing source archives only; files: `.github/workflows/release-publish.yml`; verification: `node --check src/main.js src/preload.js src/dataService.js src/renderer.js`, `npm test`.
- 2026-03-01: Added release-notes automation so published GitHub Releases include stable install guidance (including macOS quarantine-removal command) by introducing `.github/release-notes.md` and wiring `release-publish` to use `body_path`; also mirrored the same macOS note in README; files: `.github/release-notes.md`, `.github/workflows/release-publish.yml`, `README.md`; verification: `node --check src/main.js src/preload.js src/dataService.js src/renderer.js`, `npm test`.
- 2026-02-28: Hardened `.github/workflows/macos-package.yml` by replacing plain `npm ci` with a 3-attempt retry wrapper (15s backoff) to mitigate transient `socket hang up` failures during Electron dependency download on macOS runners; files: `.github/workflows/macos-package.yml`; verification: `node --check src/main.js src/preload.js src/dataService.js src/renderer.js`, `npm test`.
- 2026-02-28: Added initial public `README.md` for GitHub visitors (product overview, usage model, development commands, and latest release download guidance for Windows/macOS) and bumped package version to `1.1.1`; files: `README.md`, `package.json`, `package-lock.json`; verification: `node --check src/main.js src/preload.js src/dataService.js src/renderer.js`, `npm test`.
- 2026-02-28: Improved `Release Publish` workflow reliability/operability: fixed macOS retry step to bash (Windows remains PowerShell), added Windows retry wrapper in release workflow, and added manual `workflow_dispatch` tag input with explicit `tag_name`/release name resolution so manual runs can publish the intended version; updated release checklist accordingly; files: `.github/workflows/release-publish.yml`, `RELEASE_CHECKLIST.md`; verification: `node --check src/main.js src/preload.js src/dataService.js src/renderer.js`, `npm test`.
- 2026-02-28: Hardened Windows CI install stability by adding PowerShell retry wrappers around `npm ci` in `.github/workflows/windows-package.yml` and Windows job of `.github/workflows/release-publish.yml` (3 attempts, 15s backoff) to handle transient `socket hang up` Electron download failures; files: `.github/workflows/windows-package.yml`, `.github/workflows/release-publish.yml`; verification: `node --check src/main.js src/preload.js src/dataService.js src/renderer.js`, `npm test`.
- 2026-02-28: Added fully automated tag-based publishing workflow (`.github/workflows/release-publish.yml`): on `v*` tags it builds macOS + Windows artifacts, then creates/updates the GitHub Release and uploads artifacts automatically via `softprops/action-gh-release`; updated release/contributor docs with tag push flow; files: `.github/workflows/release-publish.yml`, `RELEASE_CHECKLIST.md`, `CONTRIBUTING.md`; verification: `node --check src/main.js src/preload.js src/dataService.js src/renderer.js`, `npm test`.
- 2026-02-28: Added repo-level safe workflow tooling and docs: new `verify`/`preflight` scripts (`scripts/verify.sh`, `scripts/preflight.sh`) wired in `package.json`, plus `CONTRIBUTING.md`, `RELEASE_CHECKLIST.md`, `.github/PULL_REQUEST_TEMPLATE.md`, and `.github/workflows/validate.yml` so code/test gates and commit hygiene are consistently enforced; files: `package.json`, `scripts/verify.sh`, `scripts/preflight.sh`, `CONTRIBUTING.md`, `RELEASE_CHECKLIST.md`, `.github/PULL_REQUEST_TEMPLATE.md`, `.github/workflows/validate.yml`; verification: `npm run verify`.
- 2026-02-28: Fixed GitHub Actions packaging failure caused by electron-builder auto-publish in CI by appending `--publish never` to macOS/Windows package commands in workflows (no PAT required for artifact-only builds); files: `.github/workflows/macos-package.yml`, `.github/workflows/windows-package.yml`; verification: `node --check src/main.js src/preload.js src/dataService.js src/renderer.js`, `npm test`.
- 2026-02-28: Restored missing npm manifests (`package.json`, `package-lock.json`) so CI packaging workflows can install dependencies (`setup-node` cache + `npm ci`) without lockfile errors; files: `package.json`, `package-lock.json`; verification: `node --check src/main.js src/preload.js src/dataService.js src/renderer.js`, `npm test`.
- 2026-02-28: Released `v1.1.0` metadata and produced macOS artifacts (`KDM Survivors Console-1.1.0-mac-arm64.dmg`, `KDM Survivors Console-1.1.0-mac-arm64.zip`) via `electron-builder`; files: `package.json`, `package-lock.json`, `release/*`; verification: `node --check src/main.js src/preload.js src/dataService.js src/renderer.js`, `npm test`, `npm run package:mac`.
- 2026-02-28: Adjusted settlement sort click behavior so selecting a different sort column now starts in descending order (instead of ascending), matching the default-desc expectation; files: `src/renderer.js`; verification: `node --check src/main.js src/preload.js src/dataService.js src/renderer.js`, `npm test`.
- 2026-02-28: Refined Create Survivor knowledge row UX by grouping `Save Template` and `Remove` together in a dedicated actions strip, and adding visually distinctive card/separator styling for each knowledge entry (with light-theme parity overrides); files: `src/renderer.js`, `ui/components/styles.css`; verification: `node --check src/main.js src/preload.js src/dataService.js src/renderer.js`, `npm test`.
- 2026-02-28: Increased light-theme visual prominence for `Ready to Ponder` by adding a dedicated `status-pill-success` override with stronger success background, border, and text contrast; files: `ui/components/styles.css`; verification: `node --check src/main.js src/preload.js src/dataService.js src/renderer.js`, `npm test`.
- 2026-02-28: Updated Create/View knowledge row rendering so `Upgrade` buttons are only shown when upgrade criteria are met (`currentObservations >= observationRequirement` and `nextKnowledgeMode !== maxLevel`), and added live visibility sync on observation requirement/current observation/mode changes; files: `src/renderer.js`; verification: `node --check src/main.js src/preload.js src/dataService.js src/renderer.js`, `npm test`.
- 2026-02-28: Fixed light-mode visibility for Settlement showdown slot selector buttons (`1`/`2`) by adding dedicated contrast styles for default, hover, active, and disabled states; files: `ui/components/styles.css`; verification: `node --check src/main.js src/preload.js src/dataService.js src/renderer.js`, `npm test`.
- 2026-02-28: Performed a light-mode contrast audit/fix pass to resolve unreadable combinations: preserved high-contrast active nav styling during hover, improved light-state text color for status pills, converted showdown control buttons to light surfaces with dark text, and overrode lingering dark philosophy/paragraph field backgrounds in light mode; files: `ui/components/styles.css`; verification: `node --check src/main.js src/preload.js src/dataService.js src/renderer.js`, `npm test`.
- 2026-02-28: Added a right-aligned navbar theme toggle (`Light Mode`/`Dark Mode`) with persisted preference (`kdm-theme`) and introduced a complementary app-wide light theme palette/surface override set (including panel/form/button/modal/table treatments and mobile nav alignment handling); files: `ui/components/index.html`, `src/renderer.js`, `ui/components/styles.css`; verification: `node --check src/main.js src/preload.js src/dataService.js src/renderer.js`, `npm test`.
- 2026-02-28: Applied a comprehensive visual redesign pass (visual-only) across app palette and core surfaces with focused UX upgrades to Showdown/Create: refined dark-fantasy color system, upgraded panel/button/input styling, sticky/showcase session bar treatment, clearer section grouping, improved row/card/toggle hierarchy, and refreshed responsive behavior for cleaner mobile/desktop rhythm; files: `ui/components/styles.css`; verification: `node --check src/main.js src/preload.js src/dataService.js src/renderer.js`, `npm test`.
- 2026-02-28: Reflowed Create/View Philosophy section for cleaner hierarchy: reduced Core Philosophy field footprint, moved and clearly labeled `Neurosis Template Name` above neurosis text, and introduced scoped philosophy layout classes for improved spacing/alignment without functional changes; files: `ui/components/index.html`, `ui/components/styles.css`; verification: `node --check src/main.js src/preload.js src/dataService.js src/renderer.js`, `npm test`.
- 2026-02-28: Added targeted Showdown spacing separation for stacked stat rows so secondary controls no longer feel cramped (notably Bleeding Tokens under vitals and Courage/Understanding ability selectors under their steppers) via top margin/padding + divider between adjacent `.showdown-stats` blocks and slight bleeding-row offset; files: `ui/components/styles.css`; verification: `node --check src/main.js src/preload.js src/dataService.js src/renderer.js`, `npm test`.
- 2026-02-28: Polished Showdown + Create/View presentation with scoped CSS refinements for spacing consistency and readability (larger inter-section rhythm, cleaner array-action/button layout, and subtle grouped section framing in Create) while preserving all existing functionality; files: `ui/components/styles.css`; verification: `node --check src/main.js src/preload.js src/dataService.js src/renderer.js`, `npm test`.
- 2026-02-28: Widened specific Create/View steppers (`Philosophy Rank`, `Next Ponder Age`, `Understanding`) to prevent label truncation/ellipsis by adding targeted `create-stepper-wide` layout overrides; files: `ui/components/index.html`, `ui/components/styles.css`; verification: `node --check src/main.js src/preload.js src/dataService.js src/renderer.js`, `npm test`.
- 2026-02-28: Added backward-compatible survivor field `philosophyNeurosisName` (schema + template default + save/load regression test) so neurosis template/source labels persist with survivors, and changed Showdown neurosis presentation from editable text/input controls to read-only display text while keeping template load/save actions; files: `src/validation/person.schema.json`, `src/dataService.js`, `src/renderer.js`, `test/dataService.test.js`, `docs/ai/PROJECT_CONTEXT.md`; verification: `node --check src/main.js src/preload.js src/dataService.js src/renderer.js`, `npm test`.
- 2026-02-28: Unified neurosis template load popup styling with knowledge popup by removing the separate neurosis modal and reusing the existing knowledge template modal in a neurosis mode (same visual shell, search/select interactions, scratch action hidden for neurosis); files: `ui/components/index.html`, `src/renderer.js`; verification: `node --check src/main.js src/preload.js src/dataService.js src/renderer.js`, `npm test`.
- 2026-02-28: Removed survivor-based default neurosis template names so create/showdown template-name inputs start blank; enforced explicit template-name requirement on save and added showdown slot-level name state retention with auto-fill from selected template when loaded; files: `src/renderer.js`; verification: `node --check src/main.js src/preload.js src/dataService.js src/renderer.js`, `npm test`.
- 2026-02-28: Polished neurosis template modal presentation to match knowledge-template UX affordances by adding a symmetric action row (`Cancel` + `Use Selected Template`) with full busy-state/event wiring; files: `ui/components/index.html`, `src/renderer.js`; verification: `node --check src/main.js src/preload.js src/dataService.js src/renderer.js`, `npm test`.
- 2026-02-28: Reworked neurosis template loading UX to match knowledge templates by adding a dedicated modal picker (search/filter + template select + apply) and replacing prompt-based neurosis template selection in both Create/View and Showdown; files: `ui/components/index.html`, `src/renderer.js`; verification: `node --check src/main.js src/preload.js src/dataService.js src/renderer.js`, `npm test`.
- 2026-02-28: Added explicit neurosis template-name inputs for save actions in Create/View and Showdown so users can directly set template names before saving (replacing prompt-only naming in those flows); files: `ui/components/index.html`, `src/renderer.js`; verification: `node --check src/main.js src/preload.js src/dataService.js src/renderer.js`, `npm test`.
- 2026-02-28: Mirrored showdown neurosis template UX into Create/View Survivor (used by settlement edits) by adding Philosophy Neurosis `Load Template` / `Save as Template` controls, wiring create-form load/save handlers to `neuroses` templates, and fixing create form render/payload sync for `philosophyNeurosis`; files: `ui/components/index.html`, `src/renderer.js`; verification: `node --check src/main.js src/preload.js src/dataService.js src/renderer.js`, `npm test`.
- 2026-02-28: Added a forward-looking breaking-change playbook for campaign resets to remove backward-compatibility code paths (legacy config fallback, tenet folder fallback, deprecated `philosophyTenet`, optional strict schemaVersion loading), including required test/doc gates; files: `docs/ai/FUTURE_PATCH_REMOVE_BACKWARD_COMPAT.md`; verification: docs-only update.
- 2026-02-28: Added neurosis template persistence/listing support with dedicated `neuroses` data source (`save-neurosis-template`, `list-neurosis-templates`) and integrated showdown neurosis template load/save controls under `Tenet Knowledge and Neurosis`; removed Philosophy Tenet inputs from Technical/Create UI while retaining JSON compatibility for existing `philosophyTenet` fields; added integration tests for neurosis template save/list and invalid-file handling; files: `src/dataService.js`, `src/main.js`, `src/preload.js`, `src/renderer.js`, `ui/components/index.html`, `ui/components/styles.css`, `test/dataService.test.js`, `docs/ai/PROJECT_CONTEXT.md`; verification: `node --check src/main.js src/preload.js src/dataService.js src/renderer.js`, `npm test`.
- 2026-02-28: Added showdown session bar `Refresh Survivors` button to reload selected survivors from settlement files into showdown memory (disabled while departed), and changed showdown-over reset flow to clear selector values and require fresh survivor reselection before next showdown; files: `ui/components/index.html`, `src/renderer.js`; verification: `node --check src/main.js src/preload.js src/dataService.js src/renderer.js`, `npm test`.
- 2026-02-28: Added visual separators between consecutive showdown `Knowledge` / `Tenet Knowledge` rows (divider + spacing) to reduce clumping when multiple entries are present; files: `ui/components/styles.css`; verification: `node --check src/main.js src/preload.js src/dataService.js src/renderer.js`, `npm test`.
- 2026-02-28: Consolidated template source resolution so `tenetKnowledge` and `knowledge` both use the configured `knowledges` data source path for list/save template actions, with legacy fallback to `tenetKnowledges` only if `knowledges` is not configured; files: `src/main.js`, `docs/ai/PROJECT_CONTEXT.md`; verification: `node --check src/main.js src/preload.js src/dataService.js src/renderer.js`, `npm test`.
- 2026-02-28: Added a `Back` button to the Create/View Survivor sticky action area (positioned above `Create/Save Survivor`) and wired it to return directly to Settlement view for quick exit when no edits are needed; files: `ui/components/index.html`, `ui/components/styles.css`, `src/renderer.js`; verification: `node --check src/main.js src/preload.js src/dataService.js src/renderer.js`, `npm test`.
- 2026-02-28: Added knowledge upgrade support to Create/Edit Survivor view by introducing row-level `Upgrade` actions for Tenet Knowledge and Knowledge entries, wiring create-mode upgrade flow through existing template picker logic, and enabling in-place replacement of upgraded entries with level-floor + observations reset behavior; files: `src/renderer.js`; verification: `node --check src/main.js src/preload.js src/dataService.js src/renderer.js`, `npm test`.
- 2026-02-28: Added inline markdown preview visibility to Showdown `Fighting Arts`, `Secret Fighting Arts`, and `Disorders` entries (no click required) via cached library preview lookup with async hydration and rerender on load; files: `src/renderer.js`, `ui/components/styles.css`; verification: `node --check src/main.js src/preload.js src/dataService.js src/renderer.js`, `npm test`.
- 2026-02-28: Added `Alive` boolean control to Showdown survivor cards (checkbox bound to `isAlive`) with change-event wiring so updates persist when showdown survivors are saved; files: `src/renderer.js`, `ui/components/styles.css`; verification: `node --check src/main.js src/preload.js src/dataService.js src/renderer.js`, `npm test`.
- 2026-02-28: Reworked Abilities/Impairments UX in Create Survivor and Showdown to use explicit commit lifecycle (`+ Add New`, draft textarea, `Save/Commit`, then read-only paragraph with `Edit`/`Remove`) while persisting arrays as plain strings; added UI-only draft state management and styling for readable paragraph layout; files: `src/renderer.js`, `ui/components/index.html`, `ui/components/styles.css`; verification: `node --check src/main.js src/preload.js src/dataService.js src/renderer.js`, `npm test`.
- 2026-02-28: Added survivor `schemaVersion` compatibility policy (current v1): load/save now normalize legacy records, reject unsupported future versions, and include migration stub in data layer; updated schema + tests for legacy and future-version handling; files: `src/dataService.js`, `src/validation/person.schema.json`, `test/dataService.test.js`, `docs/ai/PROJECT_CONTEXT.md`; verification: `node --check src/main.js src/preload.js src/dataService.js src/renderer.js`, `npm test`.
- 2026-02-28: Added mandatory AI guardrails for schema/JSON structure changes requiring explicit break-risk warning, compatibility strategy, and migration-focused tests; files: `AGENTS.md`, `docs/ai/UPDATE_CHECKLIST.md`; verification: docs-only.
- 2026-02-28: Expanded Showdown Understanding control width with a targeted stepper class so the full label fits without truncation, matching the Bleeding Tokens readability fix pattern; files: `src/renderer.js`, `ui/components/styles.css`; verification: `node --check src/main.js src/preload.js src/dataService.js src/renderer.js`, `npm test`.
- 2026-02-28: Increased Showdown Bleeding Tokens control width (double-width layout class) so full label text fits without truncation; files: `src/renderer.js`, `ui/components/styles.css`; verification: `node --check src/main.js src/preload.js src/dataService.js src/renderer.js`, `npm test`.
- 2026-02-28: Added temporary armor severity checkboxes in Showdown Armor (Head: Heavy only; Body/Arms/Waist/Legs: Light + Heavy), wired through showdown-only state and reset with slot/session resets; values are never persisted to survivor JSON; files: `src/renderer.js`, `ui/components/styles.css`; verification: `node --check src/main.js src/preload.js src/dataService.js src/renderer.js`, `npm test`.
- 2026-02-28: Added dedicated bleeding-drop SVG icon (`icon-bleeding`) and applied it to Showdown Bleeding Tokens label for clearer visual identification; files: `ui/components/index.html`, `src/renderer.js`; verification: `node --check src/main.js src/preload.js src/dataService.js src/renderer.js`, `npm test`.
- 2026-02-28: Added temporary Showdown Bleeding Tokens counter in the Age/Survival/Insanity group using showdown-only state with +/- controls and reset-on-slot/session behavior; it is not persisted to survivor JSON; files: src/renderer.js, docs/ai/PROJECT_CONTEXT.md, docs/ai/MODEL_HANDOFF.md; verification: node --check src/main.js src/preload.js src/dataService.js src/renderer.js, npm test.
- 2026-02-28: Removed cloud-sync edit-conflict artifacts from source tree (`src/renderer (# Edit conflict 2026-02-27 lzvYIYp #).js`, `src/renderer (# Edit conflict 2026-02-28 JUUF3tL #).js`) to keep `src/renderer.js` as the single canonical renderer file; files: `src/`; verification: `node --check src/main.js src/preload.js src/dataService.js src/renderer.js`, `npm test`.
- 2026-02-27: Refactored philosophy/tenet modeling: added survivor-level `philosophyNeurosis` and `philosophyTenet` fields with dedicated Philosophy sections in Technical and Create/View survivor forms (core philosophy, neurosis, tenet, philosophy rank, next ponder age), removed Neurosis/Tenet inputs from Tenet Knowledge entry editors/templates, and updated showdown Tenet Knowledge display to show only observation/rules; files: `ui/components/index.html`, `ui/components/styles.css`, `src/renderer.js`, `src/dataService.js`, `src/validation/person.schema.json`; verification: `node --check src/renderer.js src/dataService.js`, `npm run test:unit`, `npm run test:integration`.
- 2026-02-26: Polished global compact header/nav layout for 1920x1080 usage: title and nav share a single row on desktop, nav buttons no longer wrap (horizontal overflow fallback), spacing/button sizes reduced, and mobile breakpoint restores wrapping/stacked header layout; files: `ui/components/styles.css`; verification: `npm run test:unit`, `npm run test:integration`.
- 2026-02-26: Fixed showdown `Abilities` / `Impairments` add-click reliability by normalizing non-HTMLElement click targets in showdown event delegation; also applied compact showdown-style header spacing globally and moved/compacted the `Depart / Showdown Over` session controls into the header row area (CSS positioning, with mobile fallback to normal flow); files: `src/renderer.js`, `ui/components/styles.css`; verification: `node --check src/renderer.js`, `npm run test:unit`, `npm run test:integration`.
- 2026-02-26: Added process-local markdown preview LRU cache in `dataService` (keyed by absolute file path + `mtimeMs` + size) so repeated markdown library refreshes avoid rereading unchanged `.md` files while preserving current list/preview behavior; files: `src/dataService.js`; verification: `node --check src/dataService.js src/renderer.js src/main.js src/preload.js`, `npm run test:unit`, `npm run test:integration`.
- 2026-02-26: Fixed cross-platform markdown link compatibility and reduced showdown rerender overhead: markdown file IDs are normalized to forward-slash relative paths, `loadMarkdownFile` accepts legacy backslash paths, renderer duplicate checks normalize link keys, `openMarkdownFromReference` now loads directly by collection instead of relisting files, and high-frequency showdown slot mutations (stats/modifiers/armor/proficiency/obs/add-remove array items) now rerender only the affected card via `renderShowdownSlot`; files: `src/dataService.js`, `src/renderer.js`; verification: `node --check src/main.js src/preload.js src/dataService.js src/renderer.js`, `npm run test:unit`, `npm run test:integration`.
- 2026-02-26: Fixed two Data Sources markdown routing regressions in renderer: “Insert into JSON” now maps by configured collection/category (`collectionId`) instead of folder-name heuristics, and markdown array matching (`fightingArts`, `secretFightingArts`, `disorders`, `tenetKnowledge`, `knowledge`) now uses collection categories so arbitrary folder names work; files: `src/renderer.js`; verification: `node --check src/renderer.js`, `npm run test:unit`, `npm run test:integration`.
- 2026-02-26: Replaced always-visible single Data Source panel with a dedicated `Data Sources` tab and per-category folder selectors (`survivors`, `fightingArts`, `secretFightingArts`, `knowledges`, `tenetKnowledges`, `disorders`); app no longer creates folders during source selection, renderer/main/preload/dataService now use persisted category paths; files: `ui/components/index.html`, `ui/components/styles.css`, `src/renderer.js`, `src/main.js`, `src/preload.js`, `src/dataService.js`; verification: `node --check src/main.js src/preload.js src/dataService.js src/renderer.js`, `npm run test:unit`, `npm run test:integration`.
- 2026-02-26: Added `Secret Fighting Arts` as a first-class survivor array (schema + template + Technical/Create/Showdown UI + markdown picker routing) separate from `Fighting Arts`; files: `ui/components/index.html`, `src/renderer.js`, `src/dataService.js`, `src/validation/person.schema.json`; verification: `node --check src/main.js src/preload.js src/dataService.js src/renderer.js`, `npm run test:unit`, `npm run test:integration`.
- 2026-02-25: Added `Abilities` and `Impairments` sections to Create Survivor and Showdown views with free-text add/remove support; Create stores editable text rows in form state and Showdown adds text via prompt with per-item remove actions; files: `ui/components/index.html`, `src/renderer.js`, `ui/components/styles.css`; verification: `node --check src/renderer.js`, `npm run test:unit`, `npm run test:integration`.
- 2026-02-25: Fixed showdown weapon proficiency type editing by replacing static display with a compact dropdown (common proficiency types + fallback to existing custom value) and wiring change events to survivor state updates; files: `src/renderer.js`, `ui/components/styles.css`; verification: `node --check src/renderer.js`, `npm run test:unit`, `npm run test:integration`.
- 2026-02-25: Completed no-input showdown pass by converting Tenet/Knowledge `Current Observations` controls from numeric input fields to button + bold value display, matching other showdown steppers; files: `src/renderer.js`, `ui/components/styles.css`; verification: `node --check src/renderer.js`, `npm run test:unit`, `npm run test:integration`.
- 2026-02-25: Adjusted proficiency and number-display UX: weapon proficiency level now normalizes/clamps as integer (`0..8`) to avoid decimal-style ranks, showdown combat/vitals/armor/proficiency controls render as button + bold value (no numeric input boxes), and Technical/Create stepper numeric inputs are visually presented as plain numbers while still driven by +/- controls; files: `src/renderer.js`, `ui/components/styles.css`; verification: `node --check src/renderer.js`, `npm run test:unit`, `npm run test:integration`.
- 2026-02-25: Expanded Settlement table to include `Philosophy Rank`, `Weapon Proficiency`, and `Prof Rank` columns with sorting support (`philosophyRank`, `weaponProficiency.type`, `weaponProficiency.level`), and widened table min width for readability; files: `ui/components/index.html`, `src/renderer.js`, `ui/components/styles.css`; verification: `node --check src/renderer.js`, `npm run test:unit`, `npm run test:integration`.
- 2026-02-25: Refined showdown aesthetics by replacing Age/Vitals, Armor, Courage/Understanding, and Weapon Proficiency level numeric inputs with button-controlled bold value displays; values clamp to non-negative (and proficiency level remains 0..8); files: `src/renderer.js`, `ui/components/styles.css`; verification: `node --check src/renderer.js`, `npm run test:unit`, `npm run test:integration`.
- 2026-02-24: Made Data Source panel compact/top-right on desktop (responsive fallback on mobile) and added weapon proficiency editing (type + level) across Technical Visual Editor, Create/View Survivor, and Showdown survivor cards; files: `ui/components/index.html`, `ui/components/styles.css`, `src/renderer.js`; verification: `node --check src/main.js src/preload.js src/dataService.js src/renderer.js`, `npm run test:unit`, `npm run test:integration`.
- 2026-02-23: Added repo-level agent continuity instructions and AI context pack; files: `AGENTS.md`, `docs/ai/PROJECT_CONTEXT.md`, `docs/ai/MODEL_HANDOFF.md`, `docs/ai/UPDATE_CHECKLIST.md`; verification: docs-only update.
- 2026-02-23: Added setup README and packaging scripts/config for macOS/Windows; files: `README.md`, `package.json`; verification: `npm run test:unit`, `npm run test:integration`.
- 2026-02-23: Fixed local macOS manual bundle missing dependency `dist` folders by narrowing rsync excludes; files: `scripts/build-macos-local.sh`, `package.json`; verification: rebuilt `dist/macos/KDM Survivors Console.app` and confirmed `node_modules/markdown-it/dist/index.cjs.js` exists.
- 2026-02-23: Added CI workflow to build and upload Windows artifacts on GitHub Actions; files: `.github/workflows/windows-package.yml`; verification: local `npm run test:unit` pass.
- 2026-02-23: Added CI workflow to build and upload macOS artifacts on GitHub Actions; files: `.github/workflows/macos-package.yml`; verification: workflow syntax-only change (not executed locally).
- 2026-02-23: Added multi-user safety baseline (optimistic conflict detection + atomic saves + stale rename checks) and tests; files: `src/dataService.js`, `src/main.js`, `src/preload.js`, `src/validation/person.schema.json`, `src/renderer.js`, `test/dataService.test.js`; verification: `node --check src/main.js src/preload.js src/dataService.js src/renderer.js`, `npm test`.
- 2026-02-23: Optimized local mac app build size by trimming dev-only modules and excluding non-runtime folders from app payload; files: `scripts/build-macos-local.sh`; verification: rebuilt output (`dist/macos/KDM Survivors Console.app`) reduced from ~547MB to ~271MB, zip from ~219MB to ~110MB.
- 2026-02-23: Applied showdown-style stat group/stepper layout to Create Survivor view while preserving existing create IDs/events; files: `ui/components/index.html`, `ui/components/styles.css`; verification: `npm run test:unit`, `npm run test:integration`.

## Open Risks / Follow-ups
- Version 3.0.1 intentionally rejects all pre-reset survivor schemas. Users must back up old data, start a new survivor folder, and reselect current Data Sources before use.
- Renderer state complexity is high; future refactor could split large sections into modules.
- UI behavior has many coupled rules; consider adding renderer-level tests for key flows.
- Add explicit regression test for "rename existing survivor does not duplicate" at integration level.
- Linux artifacts are currently unsigned/not store-distributed; consider adding signing/notarization strategy per target distro channel if stricter trust requirements emerge.

## Next Suggested Priorities
1. Add integration tests for key renderer workflows (rename/edit, showdown depart/over, knowledge upgrade).
2. Add E2E smoke checks for view transitions and locked-slot behavior.
3. Extract showdown and knowledge template state management into dedicated modules.

## Verification Baseline
- `node --check src/main.js src/preload.js src/dataService.js src/rendererKnowledgeTemplateHelpers.js src/rendererSettlementHelpers.js src/renderer.js`
- `npm test`

## Recent Changes
- 2026-09-04: Expanded renderer regression coverage for the current roadmap by verifying departed Showdown state survives navigation through Create and Settlement without survivor reloads or slot unlocks, and by verifying bulk updates continue across individual save failures while reporting accurate aggregate outcomes; files: `test/renderer.smoke.test.js`, `docs/ai/ROADMAP.md`, `docs/ai/MODEL_HANDOFF.md`; verification: `npm run verify` (222 tests passed).
- 2026-09-04: Verified the complete public v3.1.0 release workflow and all expected platform assets, repaired Linux checksum manifests after GitHub normalized spaces in asset filenames to periods, and changed future Linux artifact names to a stable space-free form; files: `package.json`, `.github/release-notes.md`, `CHANGELOG.md`, `docs/LINUX_X64_ARM64_SUPPORT_PLAN.md`, `docs/ai/PROJECT_CONTEXT.md`, `docs/ai/MODEL_HANDOFF.md`; verification: release workflow `33918417489`, GitHub Release asset inspection, corrected public checksum manifests.
- 2026-09-04: Prepared v3.1.0 Linux release parity by making the proven native Linux matrix reusable from tagged publishing, adding x64/ARM64 tarball/DEB/RPM assets and checksum files to GitHub releases, requiring manual tags to match `package.json`, and updating version, changelog, downloads, release notes, and release guidance; files: `.github/workflows/linux-package.yml`, `.github/workflows/release-publish.yml`, `package.json`, `package-lock.json`, `CHANGELOG.md`, `README.md`, `RELEASE_CHECKLIST.md`, `.github/release-notes.md`, `docs/LINUX_X64_ARM64_SUPPORT_PLAN.md`, `docs/ai/PROJECT_CONTEXT.md`, `docs/ai/MODEL_HANDOFF.md`; verification: workflow YAML parsing, shell-path validation, `npm run verify`.
- 2026-09-04: Recorded successful installed-package acceptance of the checksum-verified `3.0.1-1.aarch64` RPM from merged-`main` workflow run `33914931154` on Fedora Linux Asahi Remix: the installed app launched sandboxed under KDE Wayland on the 16 KiB-page kernel, passed renderer/preload readiness, and completed user-confirmed functional checks with schema-v6 survivor data; files: `docs/LINUX_X64_ARM64_SUPPORT_PLAN.md`, `docs/ai/PROJECT_CONTEXT.md`, `docs/ai/MODEL_HANDOFF.md`; verification: docs-only update based on the installed package and acceptance session.
- 2026-09-04: Corrected the Linux CI unpacked-directory mapping for Electron Builder's x64 output and allowed explicit Electron arguments in the packaged smoke wrapper so hosted Xvfb checks can use `--no-sandbox`; native x64 and ARM64 verification, packaging, metadata/ELF validation, packaged-app smoke checks, and artifact uploads all passed in workflow run `33914209387`; files: `.github/workflows/linux-package.yml`, `scripts/smoke-linux-packaged.sh`, `docs/LINUX_X64_ARM64_SUPPORT_PLAN.md`, `docs/ai/MODEL_HANDOFF.md`; verification: `npm run verify`, local ARM64 packaged smoke with `--no-sandbox`, GitHub Actions run `33914209387`.
- 2026-09-04: Added a packaged Electron launch smoke mode and Linux wrapper, wired both native x64/ARM64 package jobs to launch their shipped executable under Xvfb, and verified the rebuilt ARM64 package on Fedora Asahi Wayland; files: `src/main.js`, `scripts/smoke-linux-packaged.sh`, `package.json`, `.github/workflows/linux-package.yml`, `README.md`, `docs/LINUX_X64_ARM64_SUPPORT_PLAN.md`, `docs/ai/PROJECT_CONTEXT.md`, `docs/ai/MODEL_HANDOFF.md`; verification: `npm run verify`, ARM64 package rebuild, `npm run smoke:linux:packaged -- release/linux-arm64-unpacked/kingdom-death-survivors`.
- 2026-04-21: Prepared `v2.2.2` release metadata and patch notes for the Showdown Lumi / light-theme proficiency / shadowless icon patch, including the version bump, changelog entry, and refreshed GitHub release notes; files: `package.json`, `package-lock.json`, `CHANGELOG.md`, `.github/release-notes.md`, `docs/ai/MODEL_HANDOFF.md`; verification: `node --check src/main.js src/preload.js src/dataService.js src/rendererKnowledgeTemplateHelpers.js src/rendererSettlementHelpers.js src/renderer.js`, `npm test`.
- 2026-04-21: Added a persistent Lumi counter to the Showdown vitals area beside Bleeding Tokens, wired through the existing base-stat stepper path so End Showdown saves Lumi like Survival while Bleeding Tokens remain temporary; files: `src/renderer.js`, `ui/components/styles/showdown.css`, `test/renderer.smoke.test.js`, `AGENTS.md`, `docs/ai/PROJECT_CONTEXT.md`, `docs/ai/MODEL_HANDOFF.md`; verification: `node --check src/main.js src/preload.js src/dataService.js src/rendererKnowledgeTemplateHelpers.js src/rendererSettlementHelpers.js src/renderer.js`, `npm test`.
- 2026-04-21: Fixed the Showdown weapon proficiency type input in light themes by adding targeted light/Zen theme overrides for the newer Showdown-specific proficiency input class, preventing the dark base field surface from leaking through; files: `ui/components/styles/theme-light.css`, `ui/components/styles/theme-zen.css`, `docs/ai/MODEL_HANDOFF.md`; verification: `node --check src/renderer.js`.
- 2026-04-21: Removed the baked outer shadow/fringe from the packaged app icon assets by cleaning the RGBA master and regenerating macOS, Windows, and Linux icon outputs from the shadowless master; files: `ui/assets/app-icon.png`, `ui/assets/app-icon.icns`, `ui/assets/app-icon.ico`, `ui/assets/icon-master.png`, `ui/assets/linux-icons/*`, `ui/assets/mac-icon.iconset/*`, `docs/ai/MODEL_HANDOFF.md`; verification: `iconutil --convert iconset --output /tmp/roundtrip.iconset ui/assets/app-icon.icns`.
- 2026-04-19: Prepared `v2.2.1` release metadata and patch notes for the renderer helper extraction / icon refresh update, including the version bump, changelog entry, and refreshed GitHub release notes covering expanded renderer workflow coverage, browser-safe helper seams, and rounded packaged app icons; files: `package.json`, `package-lock.json`, `CHANGELOG.md`, `.github/release-notes.md`, `docs/ai/MODEL_HANDOFF.md`; verification: `node --check src/main.js src/preload.js src/dataService.js src/rendererKnowledgeTemplateHelpers.js src/rendererSettlementHelpers.js src/renderer.js`, `npm test`, `npm run package:dir`.
- 2026-04-19: Refreshed packaged application icons across macOS, Windows, and Linux using the new `originalAppIcon.png` artwork, added a rounded-corner RGBA runtime master at `ui/assets/app-icon.png`, regenerated platform icon outputs (`.icns`, `.ico`, Linux PNG sizes, and macOS iconset`), and kept the live Electron window/dock icon aligned with the packaged assets; files: `ui/assets/app-icon.png`, `ui/assets/app-icon.icns`, `ui/assets/app-icon.ico`, `ui/assets/icon-master.png`, `ui/assets/linux-icons/*`, `ui/assets/mac-icon.iconset/*`; verification: `npm run package:dir`.
- 2026-04-19: Extended the browser-safe settlement extraction by moving settlement table rendering, search/sort/filter wiring, row-click/open behavior, row-button showdown assignment wiring, and settlement bulk-row interaction wiring into `src/rendererSettlementHelpers.js`; `src/renderer.js` now delegates settlement rendering and settlement-specific event binding to the helper while retaining state ownership and cross-surface callbacks; files: `src/rendererSettlementHelpers.js`, `src/renderer.js`, `docs/ai/ROADMAP.md`, `docs/ai/MODEL_HANDOFF.md`; verification: `node --check src/main.js src/preload.js src/dataService.js src/rendererKnowledgeTemplateHelpers.js src/rendererSettlementHelpers.js src/renderer.js test/renderer.smoke.test.js`, `npm test`.
- 2026-04-19: Extracted settlement filtering/sorting/derived-value logic into a browser-safe `src/rendererSettlementHelpers.js` file, loaded it before `renderer.js` from `ui/components/index.html`, and updated the renderer smoke harness to mirror that order; settlement rendering and event wiring remain in `renderer.js`, but the pure settlement data logic now has its own module boundary; files: `src/rendererSettlementHelpers.js`, `src/renderer.js`, `ui/components/index.html`, `test/renderer.smoke.test.js`, `docs/ai/ROADMAP.md`, `docs/ai/MODEL_HANDOFF.md`; verification: `node --check src/main.js src/preload.js src/dataService.js src/rendererKnowledgeTemplateHelpers.js src/rendererSettlementHelpers.js src/renderer.js test/renderer.smoke.test.js`, `npm test`.
- 2026-04-19: Expanded settlement-focused renderer smoke coverage by adding tests for newer/derived settlement column sorting, combined alive/ponder/ability-group filters, row-button showdown assignment swapping, and settlement-to-showdown in-memory resume behavior; also tightened the fake settlement summary payload in the smoke harness so `canPonder`, `lifetimeReroll`, and ability-group filters match the live app more closely; files: `test/renderer.smoke.test.js`, `docs/ai/ROADMAP.md`, `docs/ai/MODEL_HANDOFF.md`; verification: `node --check src/main.js src/preload.js src/dataService.js src/rendererKnowledgeTemplateHelpers.js src/renderer.js test/renderer.smoke.test.js`, `npm test`.
- 2026-04-19: Reintroduced the knowledge helper extraction using a browser-safe pattern by moving reusable knowledge template and upgrade logic into `src/rendererKnowledgeTemplateHelpers.js`, loading it before `renderer.js` from `ui/components/index.html`, and updating the renderer smoke harness to mirror that order; this preserves the live Electron boot path because it does not rely on CommonJS `require` in the browser context; files: `src/rendererKnowledgeTemplateHelpers.js`, `src/renderer.js`, `ui/components/index.html`, `test/renderer.smoke.test.js`, `docs/ai/ROADMAP.md`, `docs/ai/MODEL_HANDOFF.md`; verification: `node --check src/main.js src/preload.js src/dataService.js src/rendererKnowledgeTemplateHelpers.js src/renderer.js test/renderer.smoke.test.js`, `npm test`.
- 2026-04-19: Expanded renderer smoke coverage for the next refactor pass by adding workflow tests for edit-rename cleanup (old file removed after rename), departed showdown slot locking, and template-driven knowledge upgrades in both Create and Showdown; files: `test/renderer.smoke.test.js`, `docs/ai/ROADMAP.md`, `docs/ai/MODEL_HANDOFF.md`; verification: `node --check src/main.js src/preload.js src/dataService.js src/rendererKnowledgeTemplateHelpers.js src/renderer.js test/renderer.smoke.test.js`, `npm test`.
- 2026-04-19: Prepared `v2.2.0` release metadata and release notes for the recent reliability/safety pass, including the version bump, changelog entry, and refreshed GitHub release notes covering Settlement search debounce, showdown save hardening, and unsaved-change protection; files: `package.json`, `package-lock.json`, `CHANGELOG.md`, `.github/release-notes.md`, `docs/ai/MODEL_HANDOFF.md`; verification: `node --check src/main.js src/preload.js src/dataService.js src/renderer.js test/renderer.smoke.test.js`, `npm test`.
- 2026-04-19: Expanded renderer smoke coverage around the recent stabilization work by adding regression checks for trait-search debounce timing, default-template dirty-state clearing after save, and conflict-specific showdown partial-save messaging; completed a final verification pass with no new issues found; files: `test/renderer.smoke.test.js`, `docs/ai/MODEL_HANDOFF.md`; verification: `node --check src/main.js src/preload.js src/dataService.js src/renderer.js test/renderer.smoke.test.js`, `npm test`.
- 2026-04-19: Added unsaved-change protection to Create/Edit/default-template flows by snapshotting dirty state, showing a lightweight unsaved indicator in the create action rail, and prompting before reset, back, or page/view navigation that would discard form edits; added renderer smoke coverage for the discard-confirmation path and refreshed `docs/ai/ROADMAP.md` to mark the work complete; files: `src/renderer.js`, `ui/components/index.html`, `ui/components/styles/base.css`, `test/renderer.smoke.test.js`, `docs/ai/ROADMAP.md`, `docs/ai/MODEL_HANDOFF.md`; verification: `node --check src/main.js src/preload.js src/dataService.js src/renderer.js`, `npm test`.
- 2026-04-19: Hardened showdown end-save handling against partial-save outcomes by replacing the old all-or-nothing parallel save assumption with per-survivor result handling, syncing successful saves back into showdown memory for safe retries, and preserving departed session state with a clearer recovery message when either survivor save fails; added renderer smoke coverage for the recoverable partial-save path and refreshed `docs/ai/ROADMAP.md` to mark the work complete; files: `src/renderer.js`, `test/renderer.smoke.test.js`, `docs/ai/ROADMAP.md`, `docs/ai/MODEL_HANDOFF.md`; verification: `node --check src/main.js src/preload.js src/dataService.js src/renderer.js`, `npm test`.
- 2026-04-19: Debounced Settlement name/trait search rerenders so text filtering waits briefly before rebuilding the table, while immediate sort and non-text filter behavior stays unchanged; added renderer smoke coverage for the delayed rerender path and refreshed `docs/ai/ROADMAP.md` to record the debounce work as completed; files: `src/renderer.js`, `test/renderer.smoke.test.js`, `docs/ai/ROADMAP.md`, `docs/ai/MODEL_HANDOFF.md`; verification: `node --check src/main.js src/preload.js src/dataService.js src/renderer.js`, `npm test`.
- 2026-04-19: Prepared `v2.1.2` release metadata and patch notes for the Showdown polish update, including the version bump, changelog entry, and refreshed GitHub release notes covering split `Tokens (+)` / `Tokens (-)`, the weapon proficiency reminder/input fixes, and the reusable-template upgrade toggle fixes; files: `package.json`, `package-lock.json`, `CHANGELOG.md`, `.github/release-notes.md`, `docs/ai/PROJECT_CONTEXT.md`, `docs/ai/MODEL_HANDOFF.md`; verification: `node --check src/main.js src/preload.js src/dataService.js src/renderer.js`, `npm test`.
- 2026-04-19: Split Showdown combat `Tokens` into separate showdown-only `Tokens (+)` and `Tokens (-)` buckets, with `Tokens (+)` contributing positively to totals and `Tokens (-)` subtracting from totals; both clamp at zero and never persist to survivor files; files: `src/renderer.js`, `docs/ai/MODEL_HANDOFF.md`; verification: `node --check src/main.js src/preload.js src/dataService.js src/renderer.js`, `npm test`.
- 2026-04-19: Added a temporary Showdown weapon proficiency reminder checkbox beside the proficiency type field by narrowing the type input and storing the checkbox in showdown-only slot state; it resets with slot/session state and is never persisted to survivor JSON; files: `src/renderer.js`, `ui/components/styles/showdown.css`, `docs/ai/PROJECT_CONTEXT.md`, `docs/ai/MODEL_HANDOFF.md`; verification: `node --check src/main.js src/preload.js src/dataService.js src/renderer.js`, `npm test`.
- 2026-04-19: Prepared `v2.1.1` release metadata and patch notes for the Settlement `Ponder` summary fix, including the version bump, changelog entry, and refreshed GitHub release notes for the patch release; files: `package.json`, `package-lock.json`, `CHANGELOG.md`, `.github/release-notes.md`, `docs/ai/MODEL_HANDOFF.md`; verification: `node --check src/main.js src/preload.js src/dataService.js src/renderer.js`, `npm test`.
- 2026-04-19: Fixed a Settlement `Ponder` summary regression introduced by batch survivor summaries: `canPonder` now follows the same age-threshold rule used in survivor edit/detail views instead of also requiring non-empty philosophy text, and an integration regression test now covers threshold-ready survivors with blank philosophy fields; files: `src/dataService.js`, `test/dataService.test.js`, `docs/ai/MODEL_HANDOFF.md`; verification: `node --check src/main.js src/preload.js src/dataService.js src/renderer.js`, `npm test`.
- 2026-04-18: Prepared `v2.1.0` release metadata and patch notes for the survivor-summary refresh update, including the version bump, changelog entry, and GitHub release notes covering batch Settlement summaries and unreadable-file tolerance in shared-folder setups; files: `package.json`, `package-lock.json`, `CHANGELOG.md`, `.github/release-notes.md`, `docs/ai/MODEL_HANDOFF.md`; verification: `node --check src/main.js src/preload.js src/dataService.js src/renderer.js`, `npm test`.
- 2026-04-18: Added `docs/ai/LAN_SURVIVOR_PLAN.md` capturing the staged plan for moving survivor data to a `Local Files | LAN Host | LAN Client` model, with Settings as the connection control surface and a compact navbar connection indicator; files: `docs/ai/LAN_SURVIVOR_PLAN.md`, `docs/ai/MODEL_HANDOFF.md`; verification: docs-only update.
- 2026-04-18: Settlement refresh now uses a new batch `listPeopleSummaries` read path for table data while keeping full `loadPerson` for edit/showdown flows; summaries are generated fresh in memory per refresh, are never persisted to the shared cloud drive, and unreadable survivor files are skipped with a warning-capable payload; files: `src/dataService.js`, `src/main.js`, `src/preload.js`, `src/renderer.js`, `test/dataService.test.js`, `test/main.handlers.test.js`, `test/main.ipc.test.js`, `test/renderer.smoke.test.js`, `docs/ai/MODEL_HANDOFF.md`; verification: `node --check src/main.js src/preload.js src/dataService.js src/renderer.js`, `npm test`.
- 2026-04-18: Added `docs/ai/ROADMAP.md` to capture current high-leverage engineering follow-ups, including settlement summary loading, showdown partial-save hardening, unsaved-change protection, renderer decomposition, and renderer-focused test expansion; files: `docs/ai/ROADMAP.md`, `docs/ai/MODEL_HANDOFF.md`; verification: docs-only update.
- 2026-04-16: Restored repo-level `AGENTS.md` and consolidated current agent guidance there, including the newer UI direction toward thinner headers, less nesting, more efficient settings surfaces, and more confident/squarer control styling; files: `AGENTS.md`, `docs/ai/MODEL_HANDOFF.md`; verification: docs-only update.
